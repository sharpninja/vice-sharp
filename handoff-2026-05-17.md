# ViceSharp Handoff (2026-05-17) — ARCHIVED / SUPERSEDED

**Archived during 2026-05-27 plan audit.** This file superseded the 2026-05-12 snapshot in [handoff.md](handoff.md) only until the next slice closed. Current canonical handoff is the 2026-05-21 reboot version at repo root. See updated [handoff.md](handoff.md) and [docs/plan.md](docs/plan.md) for active Phase 1 status (HEAD 064d3a0 + post-audit reconciliation). 

Original content preserved below for history.

## Current Baseline

- 664 / 664 tests green.
- `ARCH-LOCKSTEP-001`, `ARCH-ROM-001`, and `RUNTIME-{1541,TAPE,CART,SNAPSHOT,CAPTURE}-001` remain closed.
- 100k lockstep regression gate green (`LockstepValidationTests.First100000CyclesMatch`).
- `BasicBootProofTests.C64_Boot_Reaches_Ready_Prompt` still part of the harness.

## What Landed Since 2026-05-12

- `PERF-BENCHMARK-001`: BenchmarkDotNet harness for CPU / VIC / SID / CIA / full system (commit f472eaf).
- `QA-XMLDOCS-001`: convention check + sample retrofit + Adhoc & Benchmarks tests retrofitted (commits 1846a1f, 0f9ddf6). Ratchet baseline currently 192.
- `ARCH-ADHOCMACHINE-001` and `PERF-BENCHMARK-001` marked in the README completion dashboard (commit 30788c1).
- Wiki requirements export refreshed (commit b299c41).

## Open Work (README dashboard, refreshed 2026-05-16)

- `BACKFILL-VIDEO-001` - VIC-II raster (~70%)
- `BACKFILL-SID-001` - SID voice synthesis (~55%)
- `RUNTIME-1541-002` through `RUNTIME-CAPTURE-002` - deeper subsystem gates
- `QA-XMLDOCS-001` - XMLDOCS retrofit ratchet (~7%, baseline 192)
- `REPO-MAINT-001` - dead-code audit + wiki publish (~35%)
- `ARCH-ADHOCMACHINE-001` - YAML schema + Console loader (~60%)
- `DOC-DASHBOARD-001` - completion dashboard (~50%)

## Recommended Next Slice (refreshed)

Handoff's original ordering still applies. Refined by current dashboard %:

1. **`RUNTIME-1541-002`** - real drive command / IEC protocol behavior on top of the closed `-001` gate. Biggest user-visible runtime depth gain.
2. **`BACKFILL-VIDEO-001`** - finish VIC-II raster (70% in, unlocks visible C64 boot fidelity).
3. **`QA-XMLDOCS-001`** ratchet - incremental retrofit each PR; keeps convention check green and the baseline shrinking.
4. **`ARCH-ADHOCMACHINE-001`** - finish YAML schema + Console loader (60% in; self-contained slice).
5. **`BACKFILL-SID-001`** - SID voice synthesis (55%; bigger lift, schedule after the cheaper wins).

Defer cross-platform hosts (UWP / Avalonia mobile / MacOS) until Iter-1 closes.

## Process Gates (BDP)

- Tests first. Each test XMLDOC must state the FR/TR, the use case, and the acceptance criteria.
- Validate green with mocks before real implementation.
- Full suite must stay 664+/664+ green on every slice exit.
- Preserve the 100k lockstep regression gate.
- Do NOT edit `docs/todo.yaml` directly. TODO mutations go through the MCP plugin (or REST `/mcpserver/todo` only when the plugin is unavailable).

## Validation Commands

```powershell
dotnet test .\ViceSharp.slnx --nologo
```

## Notes

- `docs/plan.md` is the consolidated plan artifact.
- This file is a midpoint handoff. When `RUNTIME-1541-002` closes, fold its results back into `handoff.md` and delete this file (or archive under `docs/session-logs/`).

## Session Log Failsafe

The pickup turn for `req-20260517T191830Z-prompt-3f1d` could not be posted to the MCP session log because the REPL agent-stdio could not authenticate. The replayable envelope is at `.dotnet-home/repl-pickup-sequence.yaml`; the status note is at `.dotnet-home/MCP_PLUGIN_UNAVAILABLE.log`. The Stop-gate hook should reconcile when the auth contract is fixed.
