<!--
  PLAN-XBOXUWP S34 (IMPL-XBOXUWP-034): the on-screen virtual C64 keyboard overlay. Binds
  VirtualKeyboardViewModel.Rows; each tile presses through the machine keyboard seam. Inert
  on the net10.0 fallback (its code-behind is #if HAS_UWP-guarded).
-->
<UserControl
    x:Class="ViceSharp.Xbox.Controls.VirtualKeyboardOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequiresPointer="Never">
    <Grid Background="#CC000000" Padding="48">
        <Border VerticalAlignment="Bottom" Background="#EE202020" CornerRadius="12" Padding="24">
            <ItemsControl ItemsSource="{Binding Rows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ItemsControl ItemsSource="{Binding}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button
                                        Content="{Binding DisplayLabel}"
                                        MinWidth="56"
                                        MinHeight="56"
                                        Margin="4"
                                        FontSize="22"
                                        Click="OnKeyPressed" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</UserControl>
