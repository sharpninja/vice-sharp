# ViceSharp Xbox UWP head: consolidated BDP v4 implementation plan

This plan fuses nine subsystem designs plus a completeness critique into one decision-complete, contradiction-free Byrd Development Process (BDP v4) plan for a NEW true UWP-on-Xbox-console head for ViceSharp (a C#/.NET 10 port of VICE, GPL-2.0-or-later). It applies the authoritative reconciliation rulings R1-R13 verbatim: any place a source section disagreed with a ruling, the ruling wins here. The head is additive: it reuses the managed core (Abstractions/Core/Chips/Architectures) and the extracted Kestrel-free host, renders with SwapChainPanel + Win2D, decouples audio through a lock-free ring buffer, drives one managed pacing gate, and is controller-only. The existing desktop build (Avalonia + WiX JIT/R2R) and the existing `src/ViceSharp.Host.Xbox` Avalonia scaffold are left byte-for-byte intact. Delivery is tests-first, requirements-driven, in small gated slices, each gate 100% green (0 failed AND 0 skipped in that slice's filter scope), determinism preserved throughout.

## 0. Decisions locked at approval (2026-07-13)

The operator approved this plan and locked these answers:
- APPROVED to begin implementation under BDP, starting at S0. Register FR/TR/TEST in MCP, commit this plan to docs/, create slice TODOs, then execute slice by slice.
- ROM acquisition PRIMARY: verified HTTPS download from the VICE ROM mirror on GitHub (VICE-Team/svn-mirror, `vice/data/C64/{basic-901226-01,kernal-901227-03,chargen-901225-01}.bin`), reusing `ViceSharp.RomFetch` `RomProvider.DownloadRom` with the SHA256 pins already at `RomProvider.cs:127-129`. USB/local import is the FALLBACK. The appxmanifest DECLARES the `internetClient` capability (R8).
- Build/signing: the operator needs a SETUP RUNBOOK (install the windows-app/UWP workload, create a self-signed dev cert, pair the Dev-Mode console via Dev Home / Device Portal). Default MSIX signing for sideload is a self-signed dev cert. The runbook is a deliverable of S0/S33 and gates the on-console slices (S0, S34, S36-S41).
- RESTORE/NMI seam: ADD it to Abstractions + the in-process host facade (FR-XKBD); do not ship the tile disabled.
- Live-gamepad deterministic replay: scoped OUT for the console head; the core stays deterministic (input pushed through the identical `SetJoystickState` seam).
- Region/fps perf target: PAL 50 (both PAL and NTSC still run).
- Deadzone/hysteresis literals: RadialDeadzone 0.30 / Activate 0.55 / Release 0.40, tunable before the S8/S9 golden-vector slices.
- `ViceSharp.Xbox.ViewModels` references Abstractions + Protocol (settings gateway uses Protocol DTOs and `SettingsOptionCatalog`); ratified.
- Gamepad `GamepadButtonFlags` uses the WinRT-identical bit layout (0x40 = DPadUp); ratified.

## 1. Locked decisions and scope

### Locked decisions (carried, not relitigated)
- Target: true UWP-on-Xbox-console app. `net10.0-windows10.0.26100.0` + `UseUwp=true`; Native AOT for Release/Store, JIT for Debug/Dev-Mode sideload.
- Operator has a Dev-Mode Xbox and a Partner Center account, so on-console validation is in scope and required for "done".
- Reuse the managed core: Abstractions, Core, Chips, Architectures (RomFetch pulled transitively).
- Controller mapping (locked): left stick + D-pad -> JOY2; right stick -> JOY1; A -> JOY2 fire; B -> JOY1 fire; View/Menu/X/Y/bumpers/triggers/stick-press -> app functions; Guide/Nexus is shell-reserved and never observed by the app.
- Existing desktop build (Avalonia + WiX JIT/R2R) stays fully intact; the Xbox head is purely additive.
- BDP v4: tests-first; FR/TR/TEST drive tests; small gated slices; each gate 0 failed AND 0 skipped in its filter scope; determinism (bit-exact replay, lockstep vs `x64sc`) preserved.

### In scope
- One new UWP head project `src/ViceSharp.Xbox` (R1) and its three supporting new libraries: `src/ViceSharp.Host.InProcess` (R2), `src/ViceSharp.Xbox.Input` (R4), `src/ViceSharp.Xbox.ViewModels` (R12).
- Gamepad reader + dual-joystick mapper; system-button + input-context state machine; Kestrel-free in-process host + single managed pacing gate; Win2D video + ring-buffered XAudio2 audio; 10-foot XAML UI with virtual C64 keyboard (including a RESTORE/NMI seam, FR-XKBD); Settings + Device Setup + first-run ROM/keymap provisioning + data-path bridge; MSIX packaging + Dev-Mode deploy + rollback + GPL compliance + CI coexistence.
- On-console validation slices (feasibility, boot/render, live gamepad, A/V, system buttons, first-run provisioning + settings persistence, performance).

### Out of scope (and explicitly NOT changing)
- `src/ViceSharp.Host.Xbox` (the Avalonia desktop-debug scaffold) is NOT modified, NOT retargeted, NOT shipped to console. Its passing `XboxHostShellTests` stays green unchanged (locked decision 6, R1). No UWP/AOT/XAML/audio/video code is added to it. The name `ViceSharp.Host.Xbox.Uwp` (system-buttons/gamepad sections) is dropped.
- The desktop Avalonia UI, WiX/winget/choco/scoop pipeline, and `ViceSharp.Console` direct-drive path are untouched.
- The core per-cycle advance path (CPU/VIC-II/SID/CIA/VIA/IEC/snapshot) is untouched; determinism and lockstep-vs-`x64sc` parity remain a desktop/test concern driven by scripted input.
- The desktop parity gate keeps using `ViceEmulationGate`/`SemaphoreEmulationGate` (kernel32 waitable-timer path) via `VICESHARP_PACE`; the new managed gate is never selected in parity runs.
- Live-gamepad deterministic bit-exact REPLAY of console input is OUT of scope for the console head (R10; see operator-confirm item 1). The core stays deterministic because input is pushed through the identical `SetJoystickState` seam.

## 2. Feasibility: the single S0 go/no-go gate (R7)

There is exactly ONE global feasibility gate, S0. It retires the one unproven unknown before any production investment. Per R7, the per-section spikes (topology `XBOXTOPO-0`, in-proc `S4`, video-audio `XAV-7`, packaging `PKG-4`, test-strategy `S0`) collapse into this single gate, and ALL production slices in ALL subsystems depend on the S0 GO decision.

S0 proves three things jointly:
1. A modern-.NET Native-AOT UWP MSIX deploys and RUNS on the physical Dev-Mode console: `dotnet publish -r win-x64 /p:PublishAot=true` of a minimal probe links clean with 0 trim/AOT (IL2xxx/IL3xxx) warnings, and the probe MSIX sideloads via Dev Mode and renders one C64 frame plus reads the gamepad once on the console.
2. The trimmed core graph links clean: the probe references Abstractions/Core/Chips/Architectures (and, for a smoke of the extracted-host shape, `Host.InProcess` once it exists; the probe itself uses direct-drive `ArchitectureBuilder.Build(descriptor).RunFrame()` per R2 so it does not block on the S1 extraction).
3. Win2D + Native-AOT packaging is clean (R13): the probe hosts a `SwapChainPanel` + Win2D (`Microsoft.Graphics.Canvas` `CanvasSwapChain`), and the AOT/MSIX package includes the per-arch Win2D native binary for win-x64 with the required rd.xml/trimming annotations and no missing-intrinsic fault.

Go/no-go criteria (all must hold for GO):
- AOT publish exit 0 with 0 IL2xxx/IL3xxx warnings under `TreatWarningsAsErrors`.
- Probe MSIX installs via Dev Mode (WinAppDeployCmd/Device Portal) and launches under Dev Home on the console.
- Managed C64 core advances at least one frame on-device (visible frame + advancing counter).
- Gamepad reads once on-device without an AppContainer/P-Invoke/marshalling fault.
- Win2D swap-chain composes and presents on-device under AOT.

If NO-GO: the head is blocked. Documented fallbacks: (a) if AOT link fails, fall back to the Debug JIT MSIX for dev deploy and treat Store/AOT as blocked pending investigation; (b) if Win2D+AOT packaging fails, S0 authorizes evaluating raw-DX11 interop (R13 rejects it only "unless S0 proves a Win2D+Native-AOT packaging blocker"). The explicit GO decision is recorded before any S1+ production slice is accepted as merged.

## 3. Target architecture

### Project inventory after this work

Existing, reused unchanged in reference shape (some gain additive content, noted):
- `ViceSharp.Abstractions` (contract; gains relocated `MasterAudioControl` + `AudioSampleConverter` per TR-XAUDIO-003, and the new RESTORE/NMI seam `IMachineKeyboardInput.SetRestoreState(bool)` per FR-XKBD).
- `ViceSharp.Core`, `ViceSharp.Chips`, `ViceSharp.Architectures`, `ViceSharp.SourceGen` (untouched).
- `ViceSharp.Protocol` (gains additive static `SettingsOptionCatalog`; DTOs/enums reused).
- `ViceSharp.RomFetch`, `ViceSharp.Monitor`, `ViceSharp.Hosting` (untouched; `ViceDataPathResolver.cs` stays byte-for-byte unchanged, bridged via env var per R9).
- `ViceSharp.Console`, `ViceSharp.Avalonia` (untouched).
- `ViceSharp.Host.Xbox` (Avalonia desktop-debug scaffold; UNTOUCHED; `XboxHostShellTests` stays green).

Refactored:
- `ViceSharp.Host` (R2): the Kestrel-free POCO service layer is EXTRACTED out into the new `ViceSharp.Host.InProcess`. `ViceSharp.Host` keeps ONLY the gRPC/Kestrel adapters (`AddViceSharpGrpcHost`/`MapViceSharpGrpcHost`, `Grpc*ServiceHost`, `GrpcHostServiceAdapters`) and references `ViceSharp.Host.InProcess`. Pure move + re-reference, determinism-neutral; the desktop gRPC host and all its tests stay green.

NEW projects:
- `src/ViceSharp.Host.InProcess` (net10.0, `IsAotCompatible`, NO `FrameworkReference Microsoft.AspNetCore.App`). Holds the moved POCO layer (namespaces preserved: `ViceSharp.Host.Services`/`ViceSharp.Host.Runtime`): `EmulatorHostService`, `EmulationPumpService`, `LocalVideoFrameSource`, `InputServiceHost`, `EmulatorRuntimeRegistry/Session`, `DefaultEmulatorRuntimeFactory`, `IEmulationGate`, `SemaphoreEmulationGate`/`ViceEmulationGate` (Windows-guarded, never selected on console), plus NEW `PacingMath`, NEW `XboxManagedFrameGate : IEmulationGate` (R3), the NEW `IConsoleEmulatorHost` facade + `ConsoleHostComposition`, and the pure audio ring/queue math + `FrameGeometry` contract.
- `src/ViceSharp.Xbox.Input` (net10.0, ZERO WinRT/Windows.Gaming.Input, `IsAotCompatible`, zero runtime reflection) (R4). Holds `GamepadButtonFlags` (WinRT-identical bits), `GamepadSnapshot` (double axes), `JoystickPortState`, `XboxInputConfig`, `XboxJoystickMapper` (pure), `XboxInputContext` (single-consumer state machine), the binding model + source-gen persistence seam, `DirectionalRepeater`.
- `src/ViceSharp.Xbox.ViewModels` (net10.0; references `ViceSharp.Abstractions` + `ViceSharp.Protocol` only; zero XAML/UWP/Core/Chips/Architectures/Host/Avalonia/Grpc) (R12). Holds all unit-testable UI logic: navigation stack, focus-graph math, virtual-keyboard layout/emission, video-pull adapter (cadence + pure sink), input-context observer, page ViewModels (Home/Settings/DeviceSetup/InputMapping/About), settings/device/ROM/UI-state ViewModels, and the seam interfaces the head adapts to.
- `src/ViceSharp.Xbox` (the UWP XAML head) (R1/R12). `TargetFramework` conditional on `ViceSharpXboxUwp` (default false -> net10.0 workload-free fallback; true -> net10.0-windows10.0.26100.0 + `UseUwp=true`). References `ViceSharp.Xbox.ViewModels` + `ViceSharp.Xbox.Input` + Abstractions/Core/Chips/Architectures + `ViceSharp.Host.InProcess`. NO reference to `ViceSharp.Host`, `ViceSharp.Avalonia`, `ViceSharp.Protocol`-Grpc, `ViceSharp.Monitor`, or any Grpc.*/Microsoft.AspNetCore.*/Avalonia.* package. Holds XAML Pages/Controls/App composition, the Win2D `SwapChainPanel` video sink, the ring-buffered XAudio2 backend, the WinRT gamepad source, the UWP `LocalDataFolder`/`StoragePicker`/`RomAcquirer`, and the adapter that implements the ViewModels seams over `IConsoleEmulatorHost`. `Package.appxmanifest` is UWP-only (glob-excluded on the fallback TFM).

### Reference rules and TFMs (exact)
- `ViceSharp.Host.InProcess`: net10.0. References Abstractions/Core/Chips/Architectures (+ RomFetch transitively). MUST NOT reference `Microsoft.AspNetCore.App` (reflection test guards it). `InternalsVisibleTo` the TestHarness for moved-type tests.
- `ViceSharp.Xbox.Input`: net10.0. References Abstractions + Protocol (for `InputPort`) only. Zero `Windows.*`/`Microsoft.AspNetCore.*`. `IsAotCompatible`, `EnableTrimAnalyzer` clean.
- `ViceSharp.Xbox.ViewModels`: net10.0. References Abstractions + Protocol + `ViceSharp.Xbox.Input` (for the shared input types the input-mapping VM surfaces). No Core/Chips/Architectures/Host/Avalonia/Grpc/XAML. (This refines R12/tenfoot "Abstractions-only": the settings gateway uses Protocol DTOs and `SettingsOptionCatalog` lives in Protocol, so Protocol is permitted; both are portable, Kestrel-free, AOT-safe. Flagged in residual items for ratification.)
- `ViceSharp.Xbox`: `ViceSharpXboxUwp` conditional TFM as above. UWP-only PropertyGroup (`'$(ViceSharpXboxUwp)'=='true'`): `UseUwp=true`, `TargetPlatformMinVersion=10.0.19041.0`, `RuntimeIdentifier=win-x64`, `Platform=x64`, `DisableRuntimeMarshalling=true`, `<PublishAot Condition="'$(Configuration)'=='Release'">true</PublishAot>`, `AppxPackage=true`, `EnableMsixTooling=true`, `WindowsPackageType=MSIX`. `EnableTrimAnalyzer`/`IsAotCompatible` inherited true (NOT overridden). Precedent: `Host.Android.csproj:24-27` (`ViceSharpHostAndroidFallback`).
- TestHarness references (R12): `ViceSharp.Xbox.Input` AND `ViceSharp.Xbox.ViewModels` (the two portable libs), plus its existing transitive reach to `ViceSharp.Host` -> `ViceSharp.Host.InProcess`. It does NOT reference the UWP head `ViceSharp.Xbox` (would drag the Windows-SDK/UWP targets and break workload-absent CI). The head's shape is validated by raw-file-read tests (`ViceSharpXboxHeadTests`/`XboxUwpHeadShellTests`), matching the HostShells precedent (`XboxHostShellTests.cs:26`, `MobileHostShellTests.cs:40`).

### Build property and AOT/trim posture (R8)
- ONE switch: MSBuild property `ViceSharpXboxUwp` (unset/false -> net10.0 fallback; true -> net10.0-windows UWP). `dotnet build ViceSharp.slnx` stays green on workload-absent agents via the fallback.
- ONE AOT mechanism: `PublishAot` gated by `Configuration=='Release'` in the csproj (Debug = JIT for fast Dev-Mode sideload). The `XboxAot.pubxml`/`XboxJitDev.pubxml` publish-profile alternative and the `ViceSharpXboxHead`/`ViceSharpXboxHead`-named property are DROPPED.
- AOT audit conclusion (grep-verified in the topology section): the reused core graph is reflection/JSON/dynamic/Emit-free. YamlDotNet in Architectures is analyzer-only static source-gen (`Architectures.csproj:15-18`). Core config must use the non-reflective INI path (never `ConfigurationBinder.Get<T>/Bind`). The only Marshal/DllImport cluster is `ViceNative.cs` (parity-test-only, never called on the Xbox path; a never-invoked DllImport is AOT-safe).

### AppContainer constraints
- No Kestrel/ASP.NET on the emulation/render/input path (guaranteed structurally by the head not referencing `ViceSharp.Host`).
- No winmm (`waveOut*`) and no kernel32 waitable-timer / `SetThreadAffinityMask` P/Invoke on the console path. Audio is XAudio2 (`xaudio2_9.dll`) via blittable vtable calls; pacing is the managed `XboxManagedFrameGate` (zero P/Invoke). The pump worker is UNPINNED (`VICESHARP_EMU_CPU` unset -> `SetThreadAffinityMask` never called).
- Filesystem writes land in `ApplicationData.Current.LocalFolder`; ROM/keymap resolution is bridged there before any host build (R9).

### Determinism guarantee
- The head reuses the managed core unchanged and only PUSHES input via `IMachineJoystickInput.SetJoystickState` (Abstractions `IMachineJoystickInput.cs:11`) / the RESTORE seam, and PULLS frames via `ILocalVideoFrameSource.TryCopyFrameInto` (`LocalVideoFrameSource.cs:82-94`). Rendering is a pure read-only sink (never advances the core). The `XboxManagedFrameGate` is a WALL-CLOCK pacing concern only (how many master cycles a Running session advances per tick); it never changes per-step cycle counts. Every gate re-runs `--filter Category=Determinism` and requires 0 failed with the pass count equal to the pre-S1 baseline.

## 4. Subsystem designs (reconciled)

### 4a. Gamepad reader + joystick mapping (area GAMEPAD; R4/R5)
Portable library `ViceSharp.Xbox.Input` owns all pure logic. WinRT decoupling: our own `[Flags] enum GamepadButtonFlags : uint` uses bit values IDENTICAL to `Windows.Gaming.Input.GamepadButtons` so the UWP adapter casts with no per-bit remap: `None=0, Menu=0x1, View=0x2, A=0x4, B=0x8, X=0x10, Y=0x20, DPadUp=0x40, DPadDown=0x80, DPadLeft=0x100, DPadRight=0x200, LeftShoulder=0x400, RightShoulder=0x800, LeftThumbstick=0x1000, RightThumbstick=0x2000, Paddle1..4=0x4000..0x20000`. No Nexus/Guide member (shell-reserved). (Reconciliation note: R4's operative rule is "WinRT-identical, cast with no per-bit remap" and "use the WinRT bit values"; that unambiguously selects this 0x40-DPadUp layout - the only one the critic confirmed is WinRT-identical. R4's parenthetical attribution to "the system-buttons section's layout" (which is 1<<10) is a mislabel and is overridden by R4's own operative rule; flagged in residual items.)

Key types and signatures:
- `public readonly record struct GamepadSnapshot(double LeftStickX, double LeftStickY, double RightStickX, double RightStickY, double LeftTrigger, double RightTrigger, GamepadButtonFlags Buttons, ulong Timestamp) { public static GamepadSnapshot Neutral => default; }` (double axes, -1..1 sticks / 0..1 triggers; mirrors `GamepadReading`).
- `public readonly record struct JoystickPortState(byte DirectionMask, bool Fire) { public static JoystickPortState Neutral => default; }` mask bits Up=0x01/Down=0x02/Left=0x04/Right=0x08 (matches `C64JoystickPort.cs:10-19`; fire is the separate bool, never a mask bit; core ORs internal Fire=0x10 itself at `C64MemoryMap.cs:597-598`).
- `public readonly record struct XboxInputConfig(double StickDeadzone, double DiagonalThreshold, double ActivateThreshold, double ReleaseThreshold, bool SwapPorts)` with exact-literal defaults frozen before S2 golden vectors (proposed: RadialDeadzone 0.30, Activate 0.55, Release 0.40; see operator-confirm item 4).
- `public static class XboxJoystickMapper { public static (JoystickPortState Joy1, JoystickPortState Joy2) Map(in GamepadSnapshot reading, in XboxInputConfig config); }` pure, deterministic, allocation-free (R5). Hysteresis latch is threaded by the single-owner `XboxInputContext` (an internal `ConverterState(byte LeftStickBits, byte RightStickBits)` prior-in/next-out overload preserves the per-axis Activate/Release hysteresis of FR-GAMEPAD-004 without static state, keeping the mapper pure).

Mapping rules: left stick OR-merged with D-pad -> Joy2; right stick -> Joy1 (no D-pad); A -> Joy2.Fire; B -> Joy1.Fire. Analog-to-8-way = radial deadzone gate (mag < deadzone releases all four bits) + per-axis Activate/Release hysteresis (removes chatter) + axis sign (+Y=Up 0x01, -Y=Down 0x02, +X=Right 0x08, -X=Left 0x04, matching the active-HIGH low nibble at `C64MemoryMap.cs:596`). SOCD applied AFTER merge: `SocdMode.Neutral` clears both bits of any Up+Down or Left+Right conflict. Swap: `XboxInputConfig.SwapPorts` flips which physical stick set feeds which explicit port INSIDE the mapper config (swap-immune; never `PrimaryJoystick`, never a downstream profile swap) (R5).

Sampling and emission: the in-process host owns the call site (R5): sample the gamepad and invoke `XboxInputContext.Tick(frameIndex, snapshot)` EXACTLY ONCE per emulated frame at the `IVideoChip.FrameCompleted` / pump Step boundary, before the next frame advances. Emission uses explicit `InputPort.Joystick1`/`Joystick2` (`ProtocolContracts.cs:57-63`, swap-immune per `InputServiceHost.cs:190-198`) via `IJoystickOutputSink`; the console sink is `DirectDriveJoystickSink` wrapping `IMachineJoystickInput.SetJoystickState` (Joystick1->control port 1, Joystick2->control port 2). Delta emission: emit to a port only when its (mask,fire) changed since the previous tick. WinRT `GamepadAdded`/`GamepadRemoved` fire on arbitrary threads and MUST NOT emit; the reader enqueues add/remove and drains at the top of `Tick` on the single emulation thread. Fail-safe: on active-pad disconnect, no pad, or a reader exception, treat the snapshot as `GamepadSnapshot.Neutral` and emit centered to BOTH ports; never throw into the frame loop.

Single-consumer boundary (R5, resolves the critique's double-path/double-read): there is NO separate `AppButtonChanged` event path and NO context-unaware pump. `XboxInputContext` is the SOLE consumer of the whole `GamepadSnapshot`; the WinRT source only produces snapshots.

WinRT adapter (head only, device-tested): `internal static GamepadSnapshot FromReading(Windows.Gaming.Input.GamepadReading r)` casting `GamepadButtons` to `GamepadButtonFlags` (identical bits) + axes + timestamp.

Failure modes: no pad -> centered once (delta-guarded); active pad lost mid-press -> center both, promote lowest-index remaining pad; `GetCurrentReading` throws -> caught/logged/swallowed -> Neutral; analog jitter -> hysteresis band; stick vs D-pad opposing -> SOCD Neutral; off-thread device events -> drained inside Tick.

### 4b. System-button + input-context state machine (areas SYSBTN, CTX; R5)
`XboxInputContext` (in `ViceSharp.Xbox.Input`) is the unified single authority (merges the gamepad pump and the system-buttons state machine per R5). It is a pure function of (previous snapshot, current snapshot, context, profile, mapper output) with no I/O, clock, or host calls.

- `public enum InputContext { Gameplay, MainMenu, VirtualKeyboard, ConfirmDialog }` (canonical; the ViewModels project maps any non-Gameplay to "UI navigation" for focus routing).
- `public enum AppCommand { None, OpenMainMenu, CloseMenu, ToggleVirtualKeyboard, AutostartDrive8, WarmReset, ColdReset, ToggleWarp, WarpHoldOn, WarpHoldOff, QuickSaveState, QuickLoadState, SwapJoystickPorts, RequestExit, ConfirmYes, ConfirmNo, UiNavigateUp/Down/Left/Right, UiActivate, UiBack }`.
- `public enum BindableInput { Menu, View, A, B, X, Y, LeftShoulder, RightShoulder, LeftTrigger, RightTrigger, LeftThumbstick, RightThumbstick }`; `public enum BindingActivation { Press, Hold, Toggle }`; `public sealed record ButtonBinding(BindableInput Input, AppCommand Command, BindingActivation Activation)`; `public sealed record BindingProfile(string Id, string DisplayName, IReadOnlyList<ButtonBinding> Gameplay) { public static BindingProfile Default { get; } }`.
- `public readonly record struct InputResolution(InputContext NextContext, IReadOnlyList<AppCommand> Commands, JoystickPortState Joy1, JoystickPortState Joy2)`.
- `public sealed class XboxInputContext { public InputContext Context { get; } public InputResolution Tick(long frameIndex, in GamepadSnapshot snapshot); }` internally calls `XboxJoystickMapper.Map` in Gameplay and forces both ports to `JoystickPortState.Neutral` (and suppresses A-fire) in every non-Gameplay context, emitting a one-shot explicit neutral push to both ports on the Gameplay->non-Gameplay edge (FR-CTX-002/004).
- `public sealed class DirectionalRepeater { public AppCommand? Tick(GamepadButtonFlags held, double elapsedMs); }` pure UI-nav auto-repeat (initial delay 400ms, repeat 90ms).

Default gameplay bindings (all remappable except Menu/View which stay menu-control): Menu = Open/Close main menu (toggle); View = Toggle virtual C64 keyboard; A = JOY2 fire (locked); B = JOY1 fire (locked); X = Autostart drive 8; Y = Warm reset (guarded by `ConfirmDialog`); LB = Quick-save; RB = Quick-load; LT hold (on>=0.6/off<=0.4 hysteresis) = Warp while held; RT = unbound; L3 = Swap ports (toggle); R3 = unbound. Exit has NO direct binding: reachable only from the main menu behind a `ConfirmDialog` (`ConfirmYes` exits). Warp maps to the limiter (`IsWarpMode` derived from `LimiterEnabled`, `AttachPanelViewModel.cs:914,931`): `WarpHoldOn` disables the limiter, `WarpHoldOff` re-enables it. App-function commands are edge-triggered (Press once per down edge; Hold On/Off across the analog threshold; Toggle flips per down edge); held buttons never auto-repeat app functions.

Persistence uses `System.Text.Json` source generation (`[JsonSerializable(typeof(BindingProfile))] public sealed partial class BindingJsonContext : JsonSerializerContext`) behind `public interface IBindingStore { BindingProfile Load(); void Save(BindingProfile profile); void ResetToDefaults(); }`; no runtime reflection. The console head binds a file-backed store rooted at `LocalFolder` (`bindings.v1.json`); tests use an in-memory store.

Dispatch decoupled from resolution: the machine only EMITS `AppCommand`s; a thin `AppCommandDispatcher` (in the head, tested against fakes) translates them to host calls through `IHostCommandSink` and marshals state-mutating commands (snapshot save/load, reset, warp) onto the session lock / mutation path (`SnapshotServiceHost.cs:37` captures under `session.SyncRoot`), never the input-polling thread. Mapping: `AutostartDrive8->ResetAndAutostartDrive8Async`, `WarmReset->WarmResetAsync`, `ColdReset->ColdResetAsync`, `QuickSaveState->CaptureSnapshotAsync`, `QuickLoadState->RestoreSnapshotAsync`, `Warp->UpdateSettings limiter toggle`, `SwapJoystickPorts->flip XboxInputConfig.SwapPorts` (no settings call), `RequestExit(ConfirmYes)->app exit`.

Failure modes: stuck direction/fire on menu open -> prevented by the one-shot neutral push (FR-CTX-004); accidental exit -> prevented by no-gameplay-exit + confirm; command spam -> edge triggering; trigger chatter -> LT hysteresis band; reflection breaking AOT -> source-gen JSON + WinRT-free portable lib; non-deterministic input -> pure `Tick` + dispatch marshaled to the session lock.

### 4c. Kestrel-free in-process host (area INPROC; R2/R3/R5)
Extract the POCO service layer into `ViceSharp.Host.InProcess` (namespace-preserving) and hand-wire it with `new` (no `WebApplication`/Kestrel). Reuse `EmulationPumpService` (dedicated worker thread, whole-instruction stepping `InstructionGroupSize=5` at `EmulationPumpService.cs:200-231`, tear-free frame commit tap `IVideoChip.FrameCompleted -> session.CommitFrame` at `EmulationPumpService.cs:247-252`), `LocalVideoFrameSource.TryCopyFrameInto` (`LocalVideoFrameSource.cs:82-94`, lock-free, non-feedback, `FR-1132`/`BUG-THROTTLE-001`), and `InputServiceHost` (push-based swap-immune injection, `InputServiceHost.cs:44-62,190-198`).

Facade and composition:
- `public interface IConsoleEmulatorHost { ConsoleSessionResult StartC64Session(ConsoleSessionOptions options); void Pause(string sessionId); void Resume(string sessionId); void ResetWarm(string sessionId); void ResetCold(string sessionId); void CloseSession(string sessionId); bool TryCopyLatestFrame(string sessionId, Span<byte> destination, out int width, out int height, out long cycle); bool TryGetFrameGeometry(string sessionId, out FrameGeometry geometry); void SetJoystick(string sessionId, ConsoleJoyPort port, byte directionMask, bool fireButton); void SetKey(string sessionId, string key, bool pressed); void SetRestoreState(string sessionId, bool pressed); }` (the last is the FR-XKBD seam).
- `public interface IConsoleDeterministicStepper { long StepFramesDeterministic(string sessionId, int frameCount); }` (test/tooling seam calling `EmulatorHostService.StepFrameAsync` under the session lock, bypassing gate/worker; NEVER used by the live UI).
- `record ConsoleSessionOptions(string ArchitectureId = "c64", bool TrueDrive = false, string? RomDataRoot = null)`; `record ConsoleSessionResult(bool Success, string SessionId, string? Error)`.
- `record FrameGeometry(int Width, int Height, int BufferLength)` where `BufferLength == IVideoChip.FrameBuffer.Length`. The concrete format is BGRA8888 384x272 (`VideoSurface.cs:18-19`, `IVideoChip.cs:24-27`), 417,792 bytes; this closes the pixel-format open item (R10/R13): the UI sizes its bitmap from `BufferLength`, never a hardcoded byte count in two places.
- `enum ConsoleJoyPort { Joystick1, Joystick2 }` fixed to `InputPort.Joystick1`(=control port 1)/`InputPort.Joystick2`(=control port 2); NEVER `PrimaryJoystick`.
- `static class ConsoleHostComposition { static ConsoleHost Build(ConsoleHostDependencies deps); }` returns `ConsoleHost : IConsoleEmulatorHost, IConsoleDeterministicStepper, IAsyncDisposable` owning pump `StartAsync`/`StopAsync`. `record ConsoleHostDependencies(IRomProvider RomProvider, IAudioBackend? AudioBackend, IReadOnlyList<IArchitectureDescriptor> Descriptors, string DefaultArchitectureId)`. It builds `DefaultEmulatorRuntimeFactory` via the explicit ctor with `ArchitectureBuilder(romProvider, audioBackend)` so it never hits the default ctor that would select `WinMmAudioBackend`.

Pacing (R3): ONE clock. `public sealed class XboxManagedFrameGate : IEmulationGate` (`Name=="Xbox"`) drives `EmulationPumpService` wall-clock pacing, replacing the winmm/kernel32 waitable-timer gate. ZERO P/Invoke; `Start()/Stop()` idempotent no-ops (no aux timer thread; critical on a 2-4 core AppContainer partition); pacing folded INLINE into `Tick` via an injectable managed sleep seam. `ctor XboxManagedFrameGate(Action<int>? sleep = null, Func<long>? nowTicks = null, long? stopwatchFrequency = null)`. Pure cycle math is extracted into `static class PacingMath` (`ComputeLimitedAdvanceCycles`/`ComputeRealtimeDeficit`, verbatim from `SemaphoreEmulationGate.cs:156-208`); `SemaphoreEmulationGate` is refactored to call `PacingMath` so both gates share one tested implementation. WARP (limiter off) BYPASSES the managed gate (runs as fast as the worker allows). Audio is a DECOUPLED sink and is NOT the clock (audio-as-clock rejected): the SID tap writes into a lock-free ring buffer; `SubmitSamples` must not block the worker. `Pause()`/suspend cleanly stops the worker and the audio drain; an OFF-console liveness test asserts `Pause()` unparks the worker with no `SubmitSamples` deadlock (safe by construction since submit is non-blocking).

Threading/determinism: inside the AppContainer only the UWP XAML UI thread and a single unpinned `ViceSharp.Emulation.Pump` worker (AboveNormal) touch emulation; `VICESHARP_EMU_CPU` unset so `AppliedWorkerAffinityMask==null`. Determinism tests bypass the gate via `StepFramesDeterministic` (single-threaded `StepFrameAsync`), exactly as existing tests do.

Failure modes: ROMs missing -> `Create` throws -> caught -> `ConsoleSessionResult(false, Error)` to Device Setup; frame before first commit -> `TryCopyFrameInto` false -> UI skips tick; destination too small -> false -> UI sizes to `BufferLength`; second `StartC64Session` -> facade closes/replaces the prior session (single-session console head).

### 4d. Video + audio pipeline (areas XVIDEO, XAUDIO, XAV; R3/R10/R13)
Video (R13): `SwapChainPanel` + Win2D (`Microsoft.Graphics.Canvas` `CanvasSwapChain`), nearest-neighbor integer upscale (`CanvasImageInterpolation.NearestNeighbor`) of the 384x272 BGRA8888 source into the 90% TV-safe rectangle (letterbox/pillarbox), ~50Hz `DispatcherQueueTimer` pull. Win2D is a WinRT component (AOT-compatible, no hand-written D3D11 COM interop). Raw-DX11 interop is rejected unless S0 proves a Win2D+AOT blocker. The pure pull adapter lives in `ViceSharp.Xbox.ViewModels`:
- `public interface ILocalVideoFramePull { bool TryCopyFrameInto(string sessionId, Span<byte> destination, out int width, out int height, out long cycle); }` (mirrors `LocalVideoFrameSource.TryCopyFrameInto`).
- `VideoFramePullViewModel` wraps it, copies into a pinned caller-owned `byte[BufferLength]` each tick, zero-alloc and lock-free, never touching the emulation lock and never calling any core-advancing member (pure sink; enforced by a forbidden-identifier scan idiom, `AvaloniaBoundaryTests.cs:51-73`). The head's `VideoSurfaceHost` uploads the buffer into a Win2D `CanvasBitmap` (`DirectXPixelFormat.B8G8R8A8UIntNormalized`, no channel swizzle) and presents. Zero-allocation gate (R10): `GC.GetAllocatedBytes` delta == 0 over a steady-state window for the frame-pull tick.

Audio (R3): `XAudio2SourceVoiceBackend : IAudioBackend, IDisposable` in the head. Native-AOT + `DisableRuntimeMarshalling` safe: `[LibraryImport("xaudio2_9.dll")]` for `XAudio2Create` only, then `IXAudio2`/`IXAudio2MasteringVoice`/`IXAudio2SourceVoice` through blittable `delegate* unmanaged` vtable pointers with blittable `WAVEFORMATEX`/`XAUDIO2_BUFFER` (same Marshal-free style as `WinMmAudioBackend.cs:20-22,398-424`). DECOUPLED ring buffer: the SID tap's `SubmitSamples` writes into a lock-free ring (non-blocking; under-run = brief silence, over-run = drop-oldest); the XAudio2 source-voice callback drains the ring on its own thread. The pure ring/queue math (`XAudio2AudioMath`: `QueuedSampleCount`, `AvailableSampleCount`, `NextFreeBufferIndex`, and a non-blocking `RoomAvailable`) lives in `ViceSharp.Host.InProcess` so the harness tests it off-console. `MasterAudioControl` + `AudioSampleConverter` are RELOCATED from `ViceSharp.Host.Audio` to `ViceSharp.Abstractions` (bodies unchanged) so the ViewModels can bind volume/mute and both desktop WinMm and Xbox XAudio2 share one master gain and one float->PCM16 path (`EffectiveGain` applied exactly once in `AudioSampleConverter`). Backend selection: `XboxAudioBackendFactory.CreateDefault()` returns a `CaptureAudioTap`-wrapped XAudio2 backend on console when a device exists (audio enabled by default on console), and `null` in headless/test (SID built with a null backend stays inert, `SidFactory.cs:37-40`, so parity/pacing runs stay silent and deterministic). Mute zeroes gain but still submits silent samples so the ring keeps flowing.

Suspend/resume (FR-XAUDIO-003): on `Suspending`, `Pause()` stops the source voice and the drain; because `SubmitSamples` is non-blocking, the worker is never parked in submit (off-console test asserts `Pause()` unparks the worker). On `Resuming`, playback continues; core state unchanged.

Failure modes: no audio endpoint -> XAudio2 open fails -> backend degrades to silent no-op, pacing continues on the managed gate, video still renders; factory null -> SID inert; ring under-run -> brief silence; frame not yet published -> keep last frame; Win2D device lost -> `UpdateFrom` returns false, retry next tick, core untouched.

### 4e. 10-foot UWP-XAML UI (area XBOXUI; R12/R13)
Two-project split (R12): all navigation, input-context observation, focus-graph math, virtual-keyboard layout/emission, video-pull adapter, and page ViewModels live in `ViceSharp.Xbox.ViewModels` (unit-tested off-console today). Only XAML Pages/Controls/App composition require the workload (S-XAML build gate) and the console (device slices).

Pages/navigation: six destinations, each a `Pages.*ViewModel` + a XAML Page/UserControl: Home/launch, In-emulator view (permanent `VideoSurfaceHost`), Settings, Device Setup, Input Mapping, About/GPL. Quick menu and virtual keyboard are overlays (visibility flags), not stack entries.
- `public enum NavigationDestination { Home, Settings, DeviceSetup, InputMapping, About }`.
- `public sealed class NavigationViewModel { NavigationDestination? Current; bool CanGoBack; void Push(NavigationDestination); bool GoBack(); bool IsQuickMenuOpen; bool IsVirtualKeyboardOpen; }` (explicit stack independent of the XAML Frame journal, unit-testable). Navigating or opening any overlay requests `XboxInputContext` to switch to a non-Gameplay context; closing all (empty stack + both overlay flags false) returns to Gameplay. The tenfoot `InputContextViewModel` is reconciled to a thin observer that reflects `XboxInputContext.Context` (the single authority, R5), not a second state machine.
- Host-facade seam (owned by the ViewModels, adapted by the head over `IConsoleEmulatorHost`): `public interface IEmulatorSessionFacade { ValueTask<string> CreateSessionAsync(CancellationToken ct=default); ValueTask StartAsync(string sessionId, CancellationToken ct=default); ValueTask PauseAsync(...); ValueTask ResumeAsync(...); ValueTask ColdResetAsync(...); ValueTask WarmResetAsync(...); ILocalVideoFramePull VideoFrames { get; } IMachineJoystickInput? GetJoystickInput(string sessionId); IMachineKeyboardInput? GetKeyboardInput(string sessionId); }`. ViewModel tests run against fakes (`FakeEmulatorSessionFacade`/`FakeVideoFramePull`); the real adapter is in the head, validated on-console.

Controller-driven 10-foot navigation: `RequiresPointer="Never"` on every interactive control (shared implicit style `FocusVisuals.xaml`); XYFocus wired from a pure `FocusMapBuilder`:
- `public readonly record struct FocusNode(string Id, string? Up, string? Down, string? Left, string? Right);`
- `public sealed class FocusMap { IReadOnlyDictionary<string, FocusNode> Nodes; }`
- `public static class FocusMapBuilder { static FocusMap BuildGrid(int rows, int columns, Func<int,int,string> idAt); }` (top-edge Up null, no wrap; graph correctness tested off-console). High-visibility focus visuals (min 4px at 1080p, scaled for 4K); single 90% TV-safe root margin; D-pad/left-stick 4-direction focus, A activate, B back. The emulator session is never implicitly paused by opening any page/overlay; the video surface and its pull timer live at the app-shell level, constructed once, never torn down by navigation (matches desktop `ShellViewModel.PauseAsync`, `ShellViewModel.cs:31`).

Virtual C64 keyboard: `VirtualKeyboardLayout` mirrors the physical C64 rows using exact `SetKeyState` strings from `C64KeyboardMap` aliases (`C64KeyboardMap.cs:13-278,170-177`: "F1".."F8", "Return"/"Enter", "Backspace"/"Del", "RunStop", "Commodore"/"Cbm", "LeftShift"/"RightShift", "Home", cursor keys, single chars). `public sealed record VirtualKeyEntry(string KeyName, string DisplayLabel, bool IsWide)`; `public sealed class VirtualKeyboardViewModel { IReadOnlyList<VirtualKeyEntry> Rows; bool ShiftLatched; void PressCurrent(); }`. A presses (`SetKeyState(key,true)` then, after a short window, `SetKeyState(key,false)`); B closes; a Shift tile (or RT) toggles a latch. RETURN and RUN/STOP get dedicated larger tiles; F1-F8 own a row with shift-latch. RESTORE tile drives the FR-XKBD seam (`SetRestoreState`), NOT `SetKeyState` (see 4h).

About/GPL page: `AboutViewModel` exposes fixed testable disclosure strings from one constants file: `LicenseIdentifier=="GPL-2.0-or-later"`, `AttributionText` contains "VICE", `SourceOfferText` non-empty with the source URL.

Failure modes: frame-pull throws / no session -> tick swallows + skips (desktop `MainWindow.axaml.cs:718-750`); page before session -> disabled placeholder, never throws; focus-graph gap -> XYFocus unset so platform falls back to candidate search; key press with no keyboard device -> `PressCurrent` no-ops; Suspend/Resume -> swap chain + pull timer torn down on `Suspending`, rebuilt on `Resuming` without losing the plain-C# ViewModels; controller disconnect -> reconnect prompt, no freeze.

### 4f. Settings + Device Setup + first-run provisioning + data-path bridge (areas XSET, XDEV, XROM; R9/R11)
Reuse the emulator settings/media/input contract; do not invent a parallel one. `SettingsOptionCatalog` (NEW static, `src/ViceSharp.Protocol/SettingsOptionCatalog.cs`, portable) single-sources the 10 option lists + `To*/From*` id<->label maps, byte-identical to `AttachPanelViewModel.cs:99-117,1021-1198` (a parity test enumerates each list). Emulator resources flow through the host `UpdateSettings` pipeline (`SettingsServiceHost.cs:488-592`) into `vice.ini`; Xbox-UI-only prefs flow to `vice-sharp.ini [ViceSharpXbox]`.

- `IXboxSettingsGateway` (portable; implemented by the head over the in-process direct client): every member is an existing `IHostProtocolClient` signature (`GetSettingsAsync`, `UpdateSettingsAsync`, `ValidateSettingsResourcesAsync`, `ListMediaAsync`, `AttachMediaAsync`, `DetachMediaAsync`, `ListKeyboardMapsAsync`, `SetKeyboardMapAsync`, `SetTrueDriveAsync`, `ListSettingsProfilesAsync`), tested against `FakeXboxSettingsGateway` (modeled on `FakeHostProtocolClient`, `AvaloniaBoundaryTests.cs:683-739`).
- `XboxSettingsViewModel`: bindable renderer/palette/scale/crop/aspect/audio-mode/input-mode/primary-joystick/swap/resource-mode/profile/limiter/pacing/master-volume/mute + Xbox-only TV-safe inset and per-stick deadzone; `RefreshAsync`/`ApplySettingsAsync(bool restart)`/`ValidateSettingsAsync`/`RevertSettings`; dirty-tracking, host-canonical adoption, restart gating (profile/resource-mode -> `RequiresRestart`).
- `XboxDeviceSetupViewModel` + `XboxMediaSlotViewModel`: four slots (Drive8 `*.d64/*.g64` trueDrive, Drive9 disk, Tape `*.tap`, Cartridge `*.crt/*.bin/*.rom`) identical to `AttachPanelViewModel.cs:57-62`; `AvailableDriveModels==DriveModelCatalog.Implemented` (1541/1540/1541-II, where `(int)C1541II==1542`); single-true-drive-rig rule + `SetTrueDriveAsync(enabled, device, diskPath, (int)model)` rebuild (`AttachPanelViewModel.cs:611-665`).
- `XboxRomProvisioningViewModel` + `RomProvisionEvaluator`: `Evaluate(c64Dir, profile) -> RomProvisionAssessment(State{NotProvisioned|Partial|Complete}, Roles[])` using `C64ViceRomNames` + `RomProvider.IsAvailable`/hash; kernal optional for Ultimax (`C64RomLoader.cs:192-193`); `IsBootBlocked` true unless Complete. Acquisition PRIMARY (locked 2026-07-13): `DownloadCoreSetAsync` downloads the 3 core ROMs over verified HTTPS from the VICE GitHub mirror (VICE-Team/svn-mirror, `vice/data/C64/{basic-901226-01,kernal-901227-03,chargen-901225-01}.bin`) via `RomProvider.DownloadRom` using the SHA256 pins already at `RomProvider.cs:127-129` (after explicit confirm; requires the `internetClient` capability). FALLBACK: `ImportFromFilesAsync` (USB/storage validated by size + MD5 via `C64RomLoader.cs:13-30,127-145`, 64MB import ceiling guard). ROMs are NEVER shipped; the 3 core C64 ROMs (basic-901226-01.bin 8KB, kernal-901227-03.bin 8KB, chargen-901225-01.bin 4KB) land in `LocalFolder\vice\C64`.

Data-path bridge and startup ordering (R9): `XboxDataPathBridge.Configure(ILocalDataFolder folder, IConfigurationBuilder? cfg)` MUST run at process entry BEFORE `ConsoleHostComposition.Build` / `DefaultEmulatorRuntimeFactory` resolves ROMs+keymaps. It creates `folder.C64Path`, sets `Environment.SetEnvironmentVariable("VICESHARP_ROM_PATH", folder.RootPath)`, sets `IConfiguration "ViceSharp:ConfigDirectory"=root`, and copies packaged `*.vkm` from `InstalledLocation\Assets\vice-data\C64\*.vkm` into `C64Path` when absent so `InputServiceHost.FindViceC64DataPath` (`InputServiceHost.cs:319-329`) finds them. `ViceDataPathResolver.cs` stays byte-for-byte unchanged (bridged only via the env var; `ContainsC64Data` at `:204` needs the created `C64` subdir). AppContainer keymap write path (R9): the `InputServiceHost` temp-file keymap writes (`InputServiceHost.cs:235,300`) are redirected to the writable `LocalFolder` root (or made no-op on console); an off-console test asserts the write path is the AppContainer-writable root.

Config source-of-truth (R9): ONE flow. Deadzone + Xbox prefs persist in `vice-sharp.ini [ViceSharpXbox]` via `XboxUiStateStore` over the portable `ViceSettings.Get/SetViceSharp/Save` (`src/ViceSharp.Core/Configuration/*`). Structured binding profiles persist as source-gen `bindings.v1.json` in `LocalFolder` AND are referenced from the INI; the converter reads deadzone from ONE resolved `XboxInputConfig`, not two competing stores. `XboxUiPrefs(SaveSettingsOnExit, SaveTransientOnExit, MasterVolumePercent, Muted, TvSafeAreaInsetPercent, LeftStickDeadzonePercent, RightStickDeadzonePercent, RomProvisionAcknowledged)`; `vice.ini` is written by the HOST from the settings DTOs, never by this subsystem.

Failure modes: no network + no USB at first run -> State stays Partial/NotProvisioned, `IsBootBlocked`, wizard re-shows; download hash mismatch -> `RomProvider.DownloadRom` throws, retry, no file written; import size/MD5 mismatch -> rejected pre-copy; vkm not copyable -> keyboard-map picker shows only the embedded gtk3_pos fallback (`InputServiceHost.cs:300-311`); `VICESHARP_ROM_PATH` unset before first resolver call -> guarded by the ordered startup step; alternate-kernal profiles (Ultimax) -> evaluator keys off `profile.KernalRomName`.

### 4g. Packaging + Dev-Mode deploy + rollback + GPL + CI (areas XBOXPKG, XBOXGPL; R8)
`Package.appxmanifest` (UWP-only, glob-excluded on the fallback TFM): `<Identity Name="sharpninja.ViceSharp.Xbox" Publisher="CN=sharpninja" ProcessorArchitecture="x64" Version="$(MsiVersion).0"/>`; `<TargetDeviceFamily Name="Windows.Xbox" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0"/>`. Capability (R8): the gamepad needs none; `internetClient` is DECLARED iff the first-run HTTPS ROM download path (FR-XROM-002) is kept. DEFAULT: USB/local import is the primary sanctioned path; HTTPS download is optional and, if kept, requires `internetClient` (a general, not restricted, capability). Do not assert "no capabilities" while keeping a download path (resolves the critique contradiction). `MsiVersion`/GitVersion (`Build.cs:879`) stamps the MSIX so it tracks the MSI/winget scheme.

GPL-2.0-or-later compliance (R8): head-only `Content` items copy `COPYING` + `THIRD_PARTY_NOTICES.md` under `Licenses/`; the shipped GPL VICE `*.vkm` keymap data is added as packaged Content under `Assets\vice-data\C64\*.vkm` WITH `THIRD_PARTY_NOTICES.md` GPL attribution (confirm vkm redistributability under GPL-2.0-or-later). The "no-ROM payload" test asserts zero `kernal-*/basic-*/chargen-*.bin` while ALLOWING the vkm assets. A compiled `SourceUrl` constant surfaces on the About screen; the GitHub release attaches the MSIX plus a `git archive` source tarball of the tag, satisfying section-3 corresponding-source for sideload (and any future Store path). Store submission is DEFERRED pending a documented GPL section-6 (no-further-restrictions) review vs the Store Standard Application License Terms.

Nuke + CI: NEW targets `PublishXbox` (AOT MSIX -> `artifacts/xbox/ViceSharp.Xbox.msix`), `DeployXbox` (WinAppDeployCmd install over the Device-Portal PIN), and `ValidateXbox` (fully off-console: fallback build + manifest XML-validate + CI-array-exclusion assert) are added to `build/Build.cs` but DELIBERATELY absent from both `DefaultPoolAzurePipelines` `InvokedTargets` arrays (`Build.cs:26,36-43`); the generated `azure-pipelines.ci.yml`/`azure-pipelines.release.yml` name no device target. Publish commands: AOT `dotnet publish src/ViceSharp.Xbox/ViceSharp.Xbox.csproj -c Release -r win-x64 -p:ViceSharpXboxUwp=true -p:PublishAot=true -p:DisableRuntimeMarshalling=true -p:GenerateAppxPackageOnBuild=true -p:AppxPackageDir=artifacts/xbox`; JIT dev `-c Debug ... -p:PublishAot=false --self-contained true`.

Rollback/uninstall (R8): the device deploy slice documents `WinAppDeployCmd uninstall` (or Device Portal remove) + redeploy of the prior MSIX; MSIX signing for sideload is a locally-trusted dev cert (`New-SelfSignedCertificate` + signtool) or Partner Center-issued.

Failure modes: CI red from a missing workload -> fallback TFM + CiConfig test; manifest rejects on console -> XML assertions catch pre-deploy; AOT link/trim failure -> off-console AOT gate catches before device; Store/GPL conflict -> avoided by sideload+source MVP; ROM accidentally packaged -> hard test failure; device targets wired into CI -> `XboxCiConfigTests` catches.

### 4h. RESTORE / NMI seam (area XKBD; R12)
There is no reliable `SetKeyState(string,bool)` that fires C64 RESTORE: it is a hardware NMI in `C64KeyboardMatrix.SetKey` (`C64KeyboardMatrix.cs:65-67`) on keycode 0x31, which the default map binds to `"*"`. Per R12 DEFAULT ruling, ADD a dedicated seam: `IMachineKeyboardInput.SetRestoreState(bool pressed)` in Abstractions, surfaced on the in-process facade as `IConsoleEmulatorHost.SetRestoreState(sessionId, bool)`, routed to the NMI path distinct from `SetKeyState`. The virtual-keyboard RESTORE tile drives this, not `SetKeyState`. Determinism preserved (NMI trigger goes through the same push seam). Fallback if the operator declines the seam: ship the RESTORE tile visibly disabled (see operator-confirm item 5).

## 5. Requirements catalog (unified R6 IDs)

Format per FR: statement; acceptance; -> mapped TR(s); -> mapped TEST(s). All IDs use the per-area slug namespace (R6).

### Area GAMEPAD (portable dual-joystick mapper)
- FR-GAMEPAD-001: Left stick and D-pad both drive JOY2, OR-merged before SOCD. Acceptance: LeftX past right threshold + DPadUp -> Joy2.DirectionMask == 0x09, JOY1 unaffected. -> TR-GAMEPAD-002 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-002: Right stick drives JOY1 with no D-pad contribution. Acceptance: RightY past up threshold -> Joy1 Up(0x01); D-pad does not alter Joy1. -> TR-GAMEPAD-002 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-003: A -> JOY2 fire, B -> JOY1 fire, emitted as the separate bool. Acceptance: Buttons=A -> Joy2.Fire only; Buttons=B -> Joy1.Fire only; no mask change. -> TR-GAMEPAD-002 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-004: Radial deadzone + per-axis Activate/Release hysteresis 8-way. Acceptance: mag 0.20 -> mask 0; 0.60 sets, 0.45 holds, 0.35 clears. -> TR-GAMEPAD-002 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-005: Axis sign +Y=Up/-Y=Down/+X=Right/-X=Left. Acceptance: the four signed extremes map to the four bits. -> TR-GAMEPAD-002 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-006: SocdMode.Neutral clears opposing pairs post-merge. Acceptance: stick Left + D-pad Right -> both Left and Right cleared. -> TR-GAMEPAD-002 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-007: Default profile is the locked mapping; remappable at runtime; swap via XboxInputConfig.SwapPorts. Acceptance: defaults LeftStick->Joystick2, RightStick->Joystick1, Dpad->Joystick2, Joy2Fire=A, Joy1Fire=B; a swapped config routes to the other port. -> TR-GAMEPAD-003 -> TEST-GAMEPAD-001.
- FR-GAMEPAD-008: Fail-safe center both ports on disconnect/no-pad/reader-exception; promote next pad. Acceptance: primary disconnect while held -> next Tick emits (Joystick1,0,false)+(Joystick2,0,false), second pad becomes primary. -> TR-GAMEPAD-004 -> TEST-GAMEPAD-001, TEST-GAMEPAD-002 (device).
- FR-GAMEPAD-009: The whole snapshot is consumed only by the single XboxInputContext (no separate app-button path). Acceptance: no `AppButtonChanged` event exists; system buttons reach the context, never a joystick port directly. -> TR-GAMEPAD-001 -> TEST-CTX-001.

TRs: TR-GAMEPAD-001 portable net10.0 lib, zero Windows.* reference, IsAotCompatible/TreatWarningsAsErrors clean (-> TEST-GAMEPAD-001 / harness build). TR-GAMEPAD-002 XboxJoystickMapper pure, no static mutable state/IO/clock, hysteresis threaded (-> TEST-GAMEPAD-001). TR-GAMEPAD-003 explicit InputPort.Joystick1/Joystick2 only, never PrimaryJoystick (-> TEST-GAMEPAD-001). TR-GAMEPAD-004 device add/remove drained on the emulation thread inside Tick (-> TEST-GAMEPAD-001). TR-GAMEPAD-005 delta emission only on change (-> TEST-GAMEPAD-001).
TESTs: TEST-GAMEPAD-001 off-console converter/merge/SOCD/fire/profile/pump behavior (0 failed/0 skipped). TEST-GAMEPAD-002 on-console live-controller smoke.

### Area SYSBTN + CTX (system buttons + input-context machine)
- FR-SYSBTN-001: Ship the default gameplay binding set. Acceptance: BindingProfile.Default enumerates the locked table by value. -> TR-SYSBTN-001 -> TEST-SYSBTN-001.
- FR-SYSBTN-002: Menu toggles the main menu; not remappable away from menu control. Acceptance: Menu edge in Gameplay -> MainMenu+OpenMainMenu; in MainMenu -> Gameplay+CloseMenu. -> TR-CTX-001 -> TEST-CTX-001.
- FR-SYSBTN-003: View toggles the virtual keyboard overlay. Acceptance: View edge toggles Gameplay<->VirtualKeyboard. -> TR-CTX-001 -> TEST-CTX-001.
- FR-SYSBTN-004: Each non-nav AppCommand maps to an existing host command. Acceptance: AppCommandDispatcher invokes the correct recorded host call once with correct args on fakes. -> TR-SYSBTN-004 -> TEST-SYSBTN-002.
- FR-SYSBTN-005: LT hold-to-warp hysteresis (on>=0.6/off<=0.4). Acceptance: rising 0.0->0.7 one WarpHoldOn; falling 0.7->0.3 one WarpHoldOff; 0.4..0.6 none. -> TR-SYSBTN-002 -> TEST-SYSBTN-001.
- FR-SYSBTN-006: SwapJoystickPorts flips XboxInputConfig.SwapPorts; no host InputSettings change. Acceptance: dispatch flips the flag, issues no ISettingsService call; second dispatch flips back. -> TR-SYSBTN-004 -> TEST-SYSBTN-002.
- FR-SYSBTN-007: Exit only from the main menu behind ConfirmDialog. Acceptance: no RequestExit binding in Default; RequestExit only from MainMenu->ConfirmDialog, only ConfirmYes exits. -> TR-CTX-001 -> TEST-CTX-001.
- FR-SYSBTN-008: Bindings remappable and persisted with reset-to-defaults. Acceptance: Save then Load round-trips an edited profile byte-for-byte; ResetToDefaults yields Default. -> TR-SYSBTN-003 -> TEST-SYSBTN-003.
- FR-CTX-001: The context machine has states Gameplay/MainMenu/VirtualKeyboard/ConfirmDialog with defined transitions. Acceptance: a transition-table test drives every (state,input) edge; no undefined transition changes context. -> TR-CTX-001 -> TEST-CTX-001.
- FR-CTX-002: Every non-Gameplay context forces both ports neutral; A does not fire in a menu. Acceptance: Resolve in any non-Gameplay context with A held + sticks deflected -> Joy1=Joy2=Neutral, no fire. -> TR-CTX-001 -> TEST-CTX-001.
- FR-CTX-003: In non-Gameplay, D-pad/left-stick -> UI nav, A=UiActivate, B=UiBack. Acceptance: DPadDown -> UiNavigateDown; A -> UiActivate; B -> UiBack; sticks emit no joystick effect. -> TR-CTX-001 -> TEST-CTX-001.
- FR-CTX-004: Entering any non-Gameplay context emits a one-shot neutralizing push to both ports. Acceptance: Gameplay->MainMenu edge -> Joy1=Joy2 neutral push (an emitted neutral, not a suppressed no-op). -> TR-CTX-001 -> TEST-CTX-001.

TRs: TR-SYSBTN-001 portable lib (no WinRT/ASP.NET, IsAotCompatible, zero reflection) (-> TEST-SYSBTN-001). TR-CTX-001 Resolve is pure/deterministic (-> TEST-CTX-001). TR-SYSBTN-002 edge-triggered activation (Press once/Hold paired/Toggle) (-> TEST-SYSBTN-001). TR-SYSBTN-003 source-gen JsonSerializerContext persistence, no reflection (-> TEST-SYSBTN-003). TR-SYSBTN-004 state-mutating dispatch marshaled onto the session lock, not the input thread (-> TEST-SYSBTN-002).
TESTs: TEST-SYSBTN-001 binding table + edge/LT-hysteresis + swap-flag (off-console). TEST-SYSBTN-002 dispatcher command->host mapping (fakes). TEST-SYSBTN-003 source-gen round-trip + reset-to-defaults + remap. TEST-CTX-001 full transition table + menu neutralization + neutral push + UI-nav mapping. TEST-SYSBTN-DEVICE on-console button response (Menu/X/LT/A gating).

### Area INPROC (Kestrel-free in-process host)
- FR-INPROC-001: The console host runs the core in-process with no Kestrel/ASP.NET on the emulation/render/input path. Acceptance: ConsoleHostComposition.Build works with no WebApplication/Kestrel; ViceSharp.Host.InProcess has no Microsoft.AspNetCore.* reference. -> TR-INPROC-001 -> TEST-INPROC-001.
- FR-INPROC-002: IConsoleEmulatorHost exposes session lifecycle + lock-free frame pull + FrameGeometry + joystick/key/RESTORE injection. Acceptance: a headless test exercises start->advance->frame-pull->input->reset->close; TryGetFrameGeometry.BufferLength == FrameBuffer.Length. -> TR-INPROC-003 -> TEST-INPROC-002.
- FR-INPROC-003: Joystick injection uses explicit Joystick1/Joystick2 (swap-immune). Acceptance: SetJoystick(Joystick2, Up|fire) -> SetJoystickState(2,0x01,true); toggling SwapJoystickPorts does not change the control port. -> TR-INPROC-003 -> TEST-INPROC-002.

TRs: TR-INPROC-001 portable net10.0 lib, IsAotCompatible, no ASP.NET FrameworkReference (-> TEST-INPROC-001). TR-INPROC-002 XboxManagedFrameGate zero P/Invoke, no aux timer thread, idempotent Start/Stop (-> TEST-INPROC-003). TR-INPROC-003 pacing does not affect emulated cycle results; deterministic advance bit-exact (-> TEST-INPROC-002). TR-INPROC-004 single unpinned pump worker, VICESHARP_EMU_CPU unset -> AppliedWorkerAffinityMask==null (-> TEST-INPROC-002). TR-INPROC-005 (R3) one managed pacing gate; audio decoupled via ring buffer, SubmitSamples non-blocking, Pause() unparks the worker (-> TEST-INPROC-003, TEST-XAUDIO-004).
TESTs: TEST-INPROC-001 assembly-reference (no ASP.NET in the portable lib) + moved-type re-run. TEST-INPROC-002 facade + determinism gate (bit-exact TotalCycles across two runs) + explicit-port + geometry + single-worker. TEST-INPROC-003 gate zero-P/Invoke + inline-sleep-seam + no-thread-growth + Pause-unparks. TEST-INPROC-DEVICE on-console boot throughput (>=100 frames/2s, unpinned, two emulation threads).

### Area XVIDEO / XAUDIO / XAV (video + audio pipeline)
- FR-XVIDEO-001: Display live C64 frames with correct BGRA colors, 4:3 letterbox, no tearing (Win2D nearest-neighbor integer scale into the TV-safe rect). Acceptance (on-console): READY. screen + animation; colors match desktop; centered/letterboxed, no tear. -> TR-XVIDEO-001 -> TEST-XVIDEO-DEVICE.
- FR-XVIDEO-002: Frame rendering is a pure read-only sink and does not perturb determinism. Acceptance: the pull adapter invokes only TryCopyFrameInto; render-enabled vs disabled replay is bit-identical; steady-state allocation delta == 0. -> TR-XVIDEO-001 -> TEST-XVIDEO-001, TEST-XVIDEO-002 (zero-alloc gate).
- FR-XAUDIO-001: SID audio plays through the console default endpoint when foreground and not muted. Acceptance (on-console): a known SID tune / $D418 tone is audible; matches desktop WinMm within resampling tolerance. -> TR-XAUDIO-001 -> TEST-XAUDIO-DEVICE.
- FR-XAUDIO-002: Master volume and mute are honored; EffectiveGain applied exactly once. Acceptance: Volume scales output; Muted silences without stopping the ring; gain applied in AudioSampleConverter before samples reach the ring. -> TR-XAUDIO-003 -> TEST-XAUDIO-003.
- FR-XAUDIO-003: Audio pauses on suspend/background and resumes without breaking determinism or deadlocking the worker. Acceptance: Suspending stops the source voice; the worker is not parked in SubmitSamples (non-blocking ring); Resuming continues; core state unchanged. -> TR-XAUDIO-002 -> TEST-XAUDIO-004, TEST-XAUDIO-DEVICE.
- FR-XAV-001: Headless/test stays silent and deterministic. Acceptance: XboxAudioBackendFactory.CreateDefault() returns null headless; SID built null-backend has IsAudioTimingSource==false and emits no samples; parity/pacing suite unchanged. -> TR-XAUDIO-001 -> TEST-XAUDIO-001.

TRs: TR-XAUDIO-001 XAudio2 backend Native-AOT + AppContainer safe (no winmm/kernel32/reflection, DisableRuntimeMarshalling-compatible blittable interop) (-> TEST-XAUDIO-001/002). TR-XAUDIO-002 (R3) audio decoupled via lock-free ring buffer; SubmitSamples non-blocking; no audio-as-clock back-pressure; Pause unblocks (-> TEST-XAUDIO-004). TR-XAUDIO-003 MasterAudioControl + AudioSampleConverter relocated to Abstractions, one master gain / one float->PCM16 shared by both backends, no behavior change (-> TEST-XAUDIO-003, PortableAudioTypesLocationTests). TR-XVIDEO-001 Win2D SwapChainPanel pure read-only sink, BGRA passthrough, zero per-frame allocation, FrameGeometry-sized (-> TEST-XVIDEO-001/002). (TR-XPACE from the source section is superseded by TR-INPROC-005; single gate.)
TESTs: TEST-XAUDIO-001 factory-null + SID inert. TEST-XAUDIO-002 XAudio2AudioMath queue/ring boundaries. TEST-XAUDIO-003 volume/mute gain-once. TEST-XAUDIO-004 non-blocking submit + Pause unparks worker (off-console; closes the critique's suspend/resume gap). TEST-XVIDEO-001 pure byte-exact copy + no-advance spy. TEST-XVIDEO-002 GC.GetAllocatedBytes delta==0 steady-state render tick. TEST-XVIDEO-DEVICE / TEST-XAUDIO-DEVICE on-console A/V smoke.

### Area XBOXUI (10-foot UWP-XAML UI)
- FR-XBOXUI-001: Six navigable destinations + quick-menu/virtual-keyboard overlays reachable from any state; back stack never recreates/pauses the running video surface. Acceptance: push Settings->DeviceSetup, GoBack twice returns to Home in order; opening overlays pushes no stack entry. -> TR-XBOXUI-001 -> TEST-XBOXUI-001.
- FR-XBOXUI-002: Every control operable by D-pad/left-stick + A/B, no pointer, high-visibility focus in the TV-safe rect. Acceptance: BuildGrid(3,2) edges correct, top Up null, no wrap; on-console focus-visual smoke. -> TR-XBOXUI-002 -> TEST-XBOXUI-002a, TEST-XBOXUI-002b (device).
- FR-XBOXUI-003: UI-nav and gameplay input never simultaneous; exactly one context authoritative. Acceptance: quick menu -> non-Gameplay; closing with empty stack -> Gameplay. -> TR-XBOXUI-003 -> TEST-XBOXUI-003.
- FR-XBOXUI-004: Session keeps running while overlays shown (video advancing unless explicitly paused). Acceptance: simulated navigation/overlay toggles never stop/dispose the pull adapter. -> TR-XBOXUI-004 -> TEST-XBOXUI-004.
- FR-XBOXUI-005: Video renders committed frames ~50Hz as a pure sink. Acceptance: 10 fires/200ms -> 10 pulls +/-1; pure-sink forbidden-identifier scan passes. -> TR-XBOXUI-005 -> TEST-XBOXUI-005a, TEST-XBOXUI-005b.
- FR-XBOXUI-006: On-screen controller-navigable C64 keyboard maps every key to the exact SetKeyState string incl RETURN/RUNSTOP/F1-F8. Acceptance: RETURN tile emits SetKeyState("Return",true) then false; shift-latch+F1 emits "F2"; every KeyName is in the known-good list. -> TR-XBOXUI-006 -> TEST-XBOXUI-006.
- FR-XBOXUI-007: ViceSharp.Xbox.ViewModels depends only on Abstractions + Protocol (portable, Kestrel-free). Acceptance: csproj-reference assertion + forbidden-identifier scan (no Core/Chips/Architectures/Host/Avalonia/Grpc/XAML). -> TR-XBOXUI-007 -> TEST-XBOXUI-007.
- FR-XBOXUI-008: About page discloses GPL-2.0-or-later, VICE attribution, source-availability offer. Acceptance: LicenseIdentifier=="GPL-2.0-or-later", AttributionText contains "VICE", SourceOfferText non-empty + URL. -> TR-XBOXUI-008 -> TEST-XBOXUI-008.

TRs: TR-XBOXUI-001 explicit navigation stack independent of the Frame journal; overlays tracked separately. TR-XBOXUI-002 RequiresPointer=Never shared style + explicit XYFocus from FocusMap + 90% safe-area root margin. TR-XBOXUI-003 InputContext observer over the single XboxInputContext authority. TR-XBOXUI-004 VideoSurfaceHost + pull timer constructed once at shell level, never disposed by navigation. TR-XBOXUI-005 SwapChainPanel + Win2D pull into a pinned buffer, nearest-neighbor into the TV-safe rect. TR-XBOXUI-006 VirtualKeyboardLayout names from the C64KeyboardMap alias set. TR-XBOXUI-007 = TR-MVVM-001 applied (Abstractions + Protocol). TR-XBOXUI-008 fixed disclosure strings from one constants file.
TESTs: TEST-XBOXUI-001..008 as above (headless), plus TEST-XBOXUI-002b device focus-visual smoke.

### Area XKBD (RESTORE / NMI)
- FR-XKBD-001: A dedicated RESTORE/NMI trigger exists on the Abstractions keyboard seam and the in-process facade; the virtual-keyboard RESTORE tile drives it, not SetKeyState. Acceptance: IMachineKeyboardInput.SetRestoreState(bool) routes to the C64 NMI path (keycode 0x31) distinct from SetKeyState; VirtualKeyboardViewModel RESTORE tile calls the facade's SetRestoreState, and a spy keyboard input records the NMI trigger, not a SetKeyState("*"). -> TR-XKBD-001 -> TEST-XKBD-001.

TRs: TR-XKBD-001 the RESTORE trigger is a distinct seam path; determinism preserved (pushed through the same input seam), no change to per-cycle advance. TESTs: TEST-XKBD-001 off-console facade + ViewModel test against a fake/spy keyboard input.

### Area XSET / XDEV / XROM (settings + device setup + provisioning)
- FR-XSET-001: Four 10-foot Settings pages bind to the same values/option ids as desktop and apply through the host UpdateSettings pipeline. Acceptance: Refresh loads GetSettings/ListSettingsProfiles; any resource change sets HasPendingSettingsChanges; Apply sends DTO ids equal to SettingsOptionCatalog.To*(...); host-canonical returned settings adopted. -> TR-XSET-002 -> TEST-XSET-001.
- FR-XSET-002: Rebuild-required changes are flagged and gated as on desktop. Acceptance: profile/resource-mode -> RequiresRestart; Apply(restart:true) sends RestartSession=true; others keep false. -> TR-XMVVM-001 -> TEST-XSET-001.
- FR-XSET-003: Validation runs without applying and surfaces per-resource results. Acceptance: Validate sends ValidateSettingsResourcesRequest, populates results, sends no UpdateSettings; editing clears stale results. -> TR-XMVVM-001 -> TEST-XSET-001.
- FR-XSET-004: RevertSettings restores the last-applied local state. Acceptance: after dirtying, Revert restores each field, clears dirty + validation. -> TR-XMVVM-001 -> TEST-XSET-001.
- FR-XSET-005: Master volume/mute/TV-safe-inset/per-stick deadzone are Xbox-only prefs persisted to vice-sharp.ini, not the emulator DTOs. Acceptance: setting them then Save writes [ViceSharpXbox]; fresh Load returns them; no UpdateSettingsRequest carries them. -> TR-XPERSIST-001 -> TEST-XSET-001.
- FR-XDEV-001: Device Setup presents Drive8/Drive9/Tape/Cartridge cards with typed attach + eject through the host media boundary. Acceptance: patterns per slot; Attach reads bytes via IStoragePicker and calls AttachMediaAsync; Eject calls DetachMediaAsync; no runtime-internal calls. -> TR-XMVVM-001 -> TEST-XSET-001.
- FR-XDEV-002: Drive cards expose the implemented drive-model selector and rebuild the true-drive rig on model change. Acceptance: AvailableDriveModels==DriveModelCatalog.Implemented; active true-drive model change calls SetTrueDriveAsync(true, device, diskPath, (int)model) with 1542 for 1541-II; inactive-slot change is inert. -> TR-XMVVM-001 -> TEST-XSET-001.
- FR-XDEV-003: Only one true-drive rig at a time. Acceptance: enabling Drive9 true-drive sets Drive8.TrueDrive=false with one rebuild; disabling all issues SetTrueDriveAsync(false). -> TR-XMVVM-001 -> TEST-XSET-001.
- FR-XROM-001: First run evaluates ROM provisioning and blocks normal boot until Complete (offering the wizard). Acceptance: Evaluate returns Complete only when every required role is present+hash-valid; NotProvisioned/Partial otherwise; IsBootBlocked unless Complete (or kernal not required). -> TR-XPATH-001 -> TEST-XSET-001.
- FR-XROM-002: The wizard acquires the core ROM set by verified HTTPS download (requires internetClient) or USB/storage import into LocalFolder\vice\C64. Acceptance: Download fetches basic/kernal/characters SHA256-checked after confirm; Import validates size+MD5 before copy; mismatch rejected leaving state unchanged. -> TR-XPATH-001 -> TEST-XSET-001.
- FR-XROM-003: Shipped GPL *.vkm keymaps are provisioned into the writable C64 folder so the picker lists them. Acceptance: after XboxDataPathBridge.Configure the packaged vkm exist under C64Path and ListKeyboardMapsAsync returns them; else the embedded gtk3_pos fallback. -> TR-XPATH-001 -> TEST-XSET-001.

TRs: TR-XPATH-001 startup path bridge makes the unmodified ViceDataPathResolver resolve the AppContainer writable root; keymap writes land in LocalFolder (-> TEST-XSET-001, XboxDataPathBridgeTests). TR-XPERSIST-001 Xbox prefs persist via the portable ViceSettings INI [ViceSharpXbox], not Avalonia SessionPersistence (-> TEST-XSET-001, XboxUiStateStoreTests). TR-XMVVM-001 Xbox ViewModels reference only Abstractions + Protocol; UWP APIs isolated behind the head (-> TEST-XBOXUI-007, boundary tests). TR-XSET-002 SettingsOptionCatalog single-sourced in Protocol, byte-identical to desktop (-> TEST-XSET-001, SettingsOptionCatalogTests).
TESTs: TEST-XSET-001 umbrella off-console suite (XboxDataPathBridgeTests, SettingsOptionCatalogTests, XboxSettingsViewModelTests, XboxDeviceSetupViewModelTests, RomProvisionEvaluatorTests, XboxRomProvisioningViewModelTests, XboxUiStateStoreTests, XboxViewModelBindabilityTests), 0 failed/0 skipped per slice filter. TEST-XSET-DEVICE on-console first-run provisioning + persistence E2E.

### Area XBOXTOPO / XBOXAOT (project topology + AOT)
- FR-XBOXTOPO-001: A new project src/ViceSharp.Xbox exists and is registered in ViceSharp.slnx as the UWP-on-console head, additive to and distinct from ViceSharp.Host.Xbox. Acceptance: csproj exists, appears in slnx, builds under default props; Host.Xbox and its test unchanged. -> TR-XBOXTOPO-001, TR-XBOXTOPO-002 -> TEST-XBOXTOPO-001, TEST-XBOXTOPO-002.

TRs: TR-XBOXTOPO-001 TFM-conditional fallback (ViceSharpXboxUwp default false=net10.0) so slnx builds with no UWP workload (-> TEST-XBOXTOPO-002). TR-XBOXTOPO-002 head references only Abstractions/Core/Chips/Architectures + Host.InProcess (R2 amendment); bans Host/Avalonia/Protocol-Grpc/Monitor and Grpc.*/AspNetCore.*/Avalonia.* (-> TEST-XBOXTOPO-001). TR-XBOXTOPO-003 (regression / desktop-intact guard, per R6 TR-XBOXTOPO-00N) existing desktop + Host.Xbox scaffold + tests stay green (-> TEST-TESTGATE-009). TR-XBOXAOT-001 core+Host.InProcess graph links clean under Native AOT, 0 IL2xxx/IL3xxx (-> TEST-XBOXAOT-001). TR-XBOXAOT-002 ViceSharpXboxUwp=true emits an MSIX whose manifest declares Windows.Xbox (-> XboxManifestTests / device). TR-XBOXAOT-003 no shipped Xbox path invokes ViceNative/WinMm/winmm-kernel32 P/Invoke (-> TEST-XBOXAOT-001).
TESTs: TEST-XBOXTOPO-001 csproj shape + reference-ban. TEST-XBOXTOPO-002 default-props solution build needs no UWP workload. TEST-XBOXAOT-001 off-console AOT/trim link gate 0 IL warnings + reflection-free audit.

### Area XBOXPKG / XBOXGPL (packaging + GPL + CI)
- FR-XBOXPKG-001: The head csproj references only the managed core (+ Host.InProcess) and none of Host/Grpc.AspNetCore/AspNetCore.App/Avalonia. Acceptance: csproj + slnx text test asserts presence of core refs + absence of forbidden refs; slnx builds. -> TR-XBOXPKG-002 -> TEST-XBOXPKG-001.
- FR-XBOXPKG-003: Package.appxmanifest declares Windows.Xbox (Min 10.0.19041.0, MaxTested 10.0.26100.0), x64 identity, and declares internetClient iff the HTTPS ROM download path is kept (else no restricted capability). Acceptance: XML-parse asserts the TargetDeviceFamily, x64, and the internetClient-present-iff-download-kept rule. -> TR-XBOXPKG-002 -> TEST-XBOXPKG-003.
- FR-XBOXGPL-006: The MSIX bundles COPYING + THIRD_PARTY_NOTICES.md (incl. vkm GPL attribution) + the *.vkm Content, bundles zero ROM *.bin, and exposes the source URL; the release attaches MSIX + source archive. Acceptance: staging test asserts Licenses/ contents + vkm Assets present + no kernal/basic/chargen *.bin + compiled SourceUrl. -> TR-XBOXCI-005 -> TEST-XBOXGPL-001.

TRs: TR-XBOXPKG-002 ViceSharpXboxUwp conditional-TFM fallback + head-only UWP/XAML/manifest/MSIX guarded so a workload-less agent builds slnx (-> TEST-XBOXPKG-001). TR-XBOXCI-005 Nuke PublishXbox/DeployXbox/ValidateXbox exist and are absent from both CI/release YAMLs; ValidateXbox runs off-console (-> TEST-XBOXCI-001). TESTs: TEST-XBOXPKG-001 csproj/slnx shape. TEST-XBOXPKG-003 manifest XML + capability rule. TEST-XBOXGPL-001 license/vkm/no-ROM/source-URL staging. TEST-XBOXCI-001 azure-pipelines.*.yml exclude device targets. TEST-XBOXDEPLOY-007 on-console install/launch/first-frame + rollback.

### Area TESTGATE (delivery-process / orchestration requirements)
- FR-TESTGATE-001: The head is delivered as an ordered BDP v4 slice sequence where S0 is a feasibility go/no-go that must pass before any production slice. Acceptance: S0 GO recorded before S1; the ordered slice list is the single source of execution order. -> TR-TESTGATE-003 -> TEST-TESTGATE-008.
- FR-TESTGATE-002: Every gate enforces 0 failed AND 0 skipped within that slice's filter scope. Acceptance: each slice trx within its --filter reports Failed=0 and Skipped=0. -> TR-TESTGATE-001 -> XboxTestConventionTests.
- FR-TESTGATE-003: Determinism and the desktop suite never regress across any gate. Acceptance: Category=Determinism 0 failed with pass count == pre-S1 baseline at every gate; Avalonia boundary + desktop build stay green. -> TR-TESTGATE-003 -> TEST-TESTGATE-009.
- FR-TESTGATE-004: The majority of gates run off-console; on-console work is isolated to marked device slices. Acceptance: production off-console slices need no hardware; only S0 + device slices are onConsole=true. -> TR-TESTGATE-004 -> slice audit.

TRs: TR-TESTGATE-001 Xbox off-console tests use plain [Fact]/[Theory] + [Trait("Category","Xbox")], never [ViceFact]/Assert.Skip, so Category=Xbox is genuinely non-skipping. TR-TESTGATE-003 the gate command set is dotnet build slnx + scoped dotnet test + dotnet publish AOT + a documented on-console checklist. TR-TESTGATE-004 head validated by raw-file-read (harness references the portable libs, not the head).
TESTs: XboxTestConventionTests (no ViceFact/Skip in Category=Xbox), TEST-TESTGATE-008 AOT proxy + on-console feasibility (= S0), TEST-TESTGATE-009 DesktopRegressionGuard, TEST-TESTGATE-010 on-console device checklists.

### Coverage
Every FR above maps to >=1 TR and >=1 TEST. Verified by area:
- GAMEPAD 001-009: all -> TR-GAMEPAD-00x -> TEST-GAMEPAD-001/002 (and FR-009 -> TEST-CTX-001).
- SYSBTN 001-008 + CTX 001-004: all -> TR-SYSBTN/TR-CTX -> TEST-SYSBTN-001/002/003 + TEST-CTX-001.
- INPROC 001-003: -> TR-INPROC-001..005 -> TEST-INPROC-001/002/003.
- XVIDEO 001-002 / XAUDIO 001-003 / XAV-001: -> TR-XVIDEO-001, TR-XAUDIO-001/002/003 -> TEST-XVIDEO-001/002, TEST-XAUDIO-001/002/003/004 + device.
- XBOXUI 001-008: -> TR-XBOXUI-001..008 -> TEST-XBOXUI-001..008.
- XKBD-001: -> TR-XKBD-001 -> TEST-XKBD-001.
- XSET 001-005 / XDEV 001-003 / XROM 001-003: -> TR-XPATH/TR-XPERSIST/TR-XMVVM/TR-XSET-002 -> TEST-XSET-001 suite.
- XBOXTOPO-001 + XBOXAOT TRs: -> TR-XBOXTOPO-001/002/003, TR-XBOXAOT-001/002/003 -> TEST-XBOXTOPO-001/002, TEST-XBOXAOT-001.
- XBOXPKG 001/003 + XBOXGPL-006: -> TR-XBOXPKG-002, TR-XBOXCI-005 -> TEST-XBOXPKG-001/003, TEST-XBOXGPL-001, TEST-XBOXCI-001.
- TESTGATE 001-004: -> TR-TESTGATE-001/003/004 -> TEST-TESTGATE-008/009/010 + XboxTestConventionTests.

No FR lacks a TR or a TEST. Gaps found and fixed during fusion: (a) the critique's orphan IDs (FR-XBOXINPUT/UI/HOST/MANIFEST/DESKTOP-INTACT) were rewired to the real per-area IDs (R6); (b) FR-XAUDIO-003 gained an off-console TEST-XAUDIO-004 (Pause unparks the worker) instead of device-only coverage (R3); (c) FR-XVIDEO-002 gained TEST-XVIDEO-002 (zero-alloc gate, R10); (d) FR-XKBD-001 + TR-XKBD-001 + TEST-XKBD-001 were added for the RESTORE seam (R12).

## 6. Byrd slice plan (one ordered global sequence)

Conventions. BUILD = `dotnet build ViceSharp.slnx -c Release`. TEST(scope) = `dotnet test tests/ViceSharp.TestHarness/ViceSharp.TestHarness.csproj -c Release --filter "<scope>" --logger "trx;LogFileName=<slice>.trx"`. DET = `dotnet test tests/ViceSharp.TestHarness/ViceSharp.TestHarness.csproj -c Release --filter "Category=Determinism"`. AOT = `dotnet publish src/ViceSharp.Xbox/ViceSharp.Xbox.csproj -c Release -r win-x64 -p:PublishAot=true -p:ViceSharpXboxUwp=false /warnaserror`. Each off-console gate requires 0 failed AND 0 skipped in its filter scope and DET unchanged from baseline. Every production slice (S1+) depends on the S0 GO decision (R7).

- S0. FEASIBILITY GO/NO-GO (the one gate all production slices depend on). onConsole=true. Dependencies: none. Tests-first: XboxAotLinkTests (off-console AOT/trim publish proxy on a minimal probe) + an on-console feasibility checklist (probe renders one C64 frame via direct-drive ArchitectureBuilder.Build(descriptor).RunFrame(), hosts a Win2D SwapChainPanel, reads the gamepad once, logs BOOTED). RED: no MSIX has run on the console; unknown whether AOT UWP MSIX + Win2D packaging deploys and the trimmed core links. GREEN: AOT publish exit 0 with 0 IL2xxx/IL3xxx warnings AND the probe MSIX sideloads via Dev Mode and shows an advancing frame + one gamepad read + a composed Win2D swap chain on the physical console. Validation: `dotnet publish <probe> -c Release -r win-x64 -p:ViceSharpXboxUwp=true -p:PublishAot=true`; `WinAppDeployCmd install -file <msix> -ip <console-ip> -pin <pin>`; on-console checklist. Acceptance: the three go/no-go criteria of section 3 recorded GREEN; explicit GO recorded before S1.

- S1. Extract ViceSharp.Host.InProcess (namespace-preserving); Host keeps only Grpc/Kestrel. onConsole=false. Dependencies: S0. Tests-first: TEST-INPROC-001 (reflection: typeof(EmulationPumpService).Assembly == ViceSharp.Host.InProcess and its referenced assemblies contain no Microsoft.AspNetCore.*) + re-run existing EmulationPumpService/LocalVideoFrameSource/InputServiceHost tests from the new location + existing InProcessGrpcHost tests. RED: the library does not exist; POCO types still resolve to ViceSharp.Host (which references AspNetCore). GREEN: POCO service/runtime files moved with namespaces unchanged; ViceSharp.Host references it and retains only Grpc*ServiceHost + GrpcHostServiceAdapters + Kestrel; whole solution builds under TreatWarningsAsErrors; all pre-existing + Host.Xbox tests green. Validation: BUILD; TEST("FullyQualifiedName~Host"); DET. Acceptance: no ASP.NET reference in the portable lib; desktop gRPC host still starts; DET == baseline.

- S2. PacingMath extraction + XboxManagedFrameGate (single managed gate, zero P/Invoke) (R3). onConsole=false. Dependencies: S1. Tests-first: TEST-INPROC-003 group: gate implements IEmulationGate Name=="Xbox"; Start()/Stop() idempotent + add no threads; reflection asserts no [DllImport]/[LibraryImport]; Tick on a Running limited session advances >0 via a fake advance + returns true, Stopped/empty returns false, sleep seam captured (no real sleep); warp session (LimiterEnabled=false) bypasses the blocking sleep; PacingMath.ComputeLimitedAdvanceCycles parity vs the former SemaphoreEmulationGate table. RED: XboxManagedFrameGate/PacingMath absent; SemaphoreEmulationGate owns the math privately. GREEN: PacingMath holds the pure math, SemaphoreEmulationGate delegates to it (behavior unchanged), the gate paces inline with injectable sleep/clock seams and passes all cases. Validation: TEST("FullyQualifiedName~EmulationGate|FullyQualifiedName~PacingMath"); DET. Acceptance: no new OS timer thread; SemaphoreEmulationGate existing pacing tests still green; DET == baseline.

- S3. Kestrel-free composition root + IConsoleEmulatorHost facade + FrameGeometry + per-frame input hook + determinism gate. onConsole=false. Dependencies: S1, S2. Tests-first: TEST-INPROC-002 group: Build(deps) returns a facade; StartC64Session Success + non-empty SessionId; StepFramesDeterministic(N) twice yields identical Clock.TotalCycles + MachineState == committed golden with gate/pump never invoked; SetJoystick(Joystick2,0x01,true) applies SetJoystickState(2,0x01,true) and SwapJoystickPorts leaves control port 2; TryCopyLatestFrame false before first commit then true with width/height, too-small destination false, TryGetFrameGeometry.BufferLength==FrameBuffer.Length; after start exactly one worker + AppliedWorkerAffinityMask==null; a per-frame input hook (invoked at FrameCompleted/Step boundary) is exposed for the head to wire XboxInputContext.Tick. RED: facade/DTOs/hook absent; tests fail to compile. GREEN: facade hand-wires the POCO graph with new (no WebApplication), starts the pump on first session, routes lifecycle/frame/input, exposes the FrameCompleted per-frame hook, and passes the bit-exact determinism gate. Validation: TEST("FullyQualifiedName~ConsoleEmulatorHost|FullyQualifiedName~InProcHost"); DET. Acceptance: no ASP.NET/Kestrel type referenced by the composition; determinism golden reproduced; DET == baseline.

- S4. Create ViceSharp.Xbox head project (ViceSharpXboxUwp fallback, core+Host.InProcess refs, slnx, solution-build gate). onConsole=false. Dependencies: S1, S0. Tests-first: TEST-XBOXTOPO-001 (Csproj_HasConditionalTfm_AndCoreReferences, Csproj_HasNoBannedReferences incl. Host/Avalonia/Protocol-Grpc/Monitor + Grpc.*/AspNetCore.*/Avalonia.*) + TEST-XBOXTOPO-002 (default-props solution build needs no UWP workload) + XboxHostShellTests still green. RED: src/ViceSharp.Xbox/ViceSharp.Xbox.csproj does not exist. GREEN: new csproj with the ViceSharpXboxUwp conditional TFM (default net10.0), references Abstractions/Core/Chips/Architectures + Host.InProcess, no banned refs; registered in slnx; solution builds with no workload; new tests pass; Host.Xbox untouched and its test green. Validation: BUILD; TEST("FullyQualifiedName~XboxUwpHead"); TEST("FullyQualifiedName~XboxHostShell"). Acceptance: slnx build exit 0 without the UWP workload; reference-ban green.

- S5. Off-console AOT/trim link gate on the reused core+Host.InProcess graph. onConsole=false. Dependencies: S4. Tests-first: TEST-XBOXAOT-001 (XboxAotLinkTests static reflection-free audit over shipped source excluding ViceNative.cs) + an AOT publish gate asserting 0 IL trim/AOT warnings on the net10.0 fallback. RED: a stray reflection/JSON/dynamic path or a shipped ViceNative/WinMm call would surface IL2xxx/IL3xxx. GREEN: ILC link completes with 0 trim/AOT warnings; reflection-free audit passes; ViceNative confirmed uncalled on the Xbox path. Validation: AOT; TEST("FullyQualifiedName~XboxAotLink"). Acceptance: publish exit 0, 0 IL warnings; no runtime reflection/JSON/dynamic in the shipped core graph.

- S6. Relocate MasterAudioControl + AudioSampleConverter to ViceSharp.Abstractions (R3-adjacent; enables ViewModels volume/mute binding). onConsole=false. Dependencies: S1. Tests-first: PortableAudioTypesLocationTests (both types' Assembly == typeof(IAudioBackend).Assembly) + existing MasterAudioControlTests/SidAudioBackendTests/SidAudioWiringTests as green guards. RED: types still in ViceSharp.Host.Audio. GREEN: types live in Abstractions (bodies unchanged); WinMmAudioBackend.cs:262,269 + AudioBackendFactory usings updated; solution builds; a project referencing only Abstractions can use MasterAudioControl. Validation: BUILD; TEST("FullyQualifiedName~Audio.MasterAudioControl|FullyQualifiedName~PortableAudioTypesLocation|FullyQualifiedName~SidAudioBackend|FullyQualifiedName~Audio.SidAudioWiring"). Acceptance: no behavior change; desktop WinMm path unaffected.

- S7. ViceSharp.Xbox.Input scaffold + POCO data shapes (R4). onConsole=false. Dependencies: S0. Tests-first: GamepadDataShapeTests (GamepadButtonFlags WinRT-identical bit values A=0x4,B=0x8,DPadUp=0x40,...; GamepadSnapshot.Neutral all-zero; JoystickPortState.Neutral (0,false)) + a portability assertion (net10.0, IsAotCompatible, TreatWarningsAsErrors, zero Windows.* reference). RED: ViceSharp.Xbox.Input and its types do not exist; harness fails to compile. GREEN: the library exists with the enums/structs; harness references it; data-shape asserts pass. Validation: `dotnet build src/ViceSharp.Xbox.Input/ViceSharp.Xbox.Input.csproj -c Release`; TEST("FullyQualifiedName~GamepadDataShape"). Acceptance: GamepadButtonFlags byte-identical to Windows.Gaming.Input.GamepadButtons; zero Windows.* reference.

- S8. Pure analog->8-way converter (deadzone + hysteresis + axis sign). onConsole=false. Dependencies: S7. Tests-first: XboxJoystickMapper converter tests (deadzone gate, axis sign, Activate/Release hysteresis, diagonal, purity: identical inputs -> identical outputs, latch threaded). RED: mapper returns placeholder. GREEN: radial-deadzone gate + per-axis hysteresis for both sticks with latch threaded; converter asserts pass (FR-GAMEPAD-004/005; TR-GAMEPAD-002). Validation: TEST("FullyQualifiedName~GamepadConverter"). Acceptance: purity assert green; no static mutable state.

- S9. Merge + SOCD + fire + swap-in-config + profile default/remap. onConsole=false. Dependencies: S8. Tests-first: merge/SOCD/fire tests (L stick Right + DPadUp -> Joy2 0x09, JOY1 unaffected; R stick Up -> Joy1 0x01; opposing -> both cleared; A->Joy2.Fire only, B->Joy1.Fire only) + profile default/remap + XboxInputConfig.SwapPorts routing. RED: Map ignores profile/D-pad merge/SOCD/fire. GREEN: Map consumes the config, OR-merges D-pad into the left-stick target port, applies SocdMode.Neutral post-merge, sets per-port fire, and honors SwapPorts (FR-GAMEPAD-001/002/003/006/007). Validation: TEST("FullyQualifiedName~GamepadMergeSocd|FullyQualifiedName~GamepadProfile"). Acceptance: all merge/SOCD/fire/profile asserts pass.

- S10. System-button binding model + edge activation (BindingProfile.Default, LT hysteresis, Toggle). onConsole=false. Dependencies: S7. Tests-first: TEST-SYSBTN-001 (BindingProfile.Default full table; Press fires once across a held sequence; Toggle flips per down edge; LT one WarpHoldOn 0.0->0.7 + one WarpHoldOff 0.7->0.3, none in 0.4..0.6; SwapPorts flips XboxInputConfig.SwapPorts). RED: binding model + edge/activation evaluator absent. GREEN: model + evaluator implemented; all S10 tests pass (FR-SYSBTN-001/005/006; TR-SYSBTN-002). Validation: `dotnet build src/ViceSharp.Xbox.Input/ViceSharp.Xbox.Input.csproj`; TEST("FullyQualifiedName~XboxSystemButtonsBindingTests"). Acceptance: Default matches the locked table by value; held Press-bound button emits once; LT emits exactly one On + one Off.

- S11. XboxInputContext single-consumer state machine (R5; merges gamepad pump + context gating). onConsole=false. Dependencies: S8, S9, S10. Tests-first: TEST-CTX-001 (full transition table over the four contexts; A held + sticks deflected in every non-Gameplay context -> Joy1=Joy2=Neutral, no fire; Gameplay->MainMenu edge emits a one-shot neutral push; DPad/A/B -> UiNavigate*/UiActivate/UiBack in menus; Menu/View toggles; DirectionalRepeater initial-delay/repeat with injected elapsed-ms; determinism: identical inputs -> equal InputResolution) + the single-consumer contract (Tick once per frame drives both mapping and system-button edges; no separate app-button path). RED: XboxInputContext/InputResolution/DirectionalRepeater absent. GREEN: pure Tick + gating + neutral push + UI-nav intents implemented; all cases pass (FR-CTX-001/002/003/004; FR-SYSBTN-002/003/007; FR-GAMEPAD-009; TR-CTX-001). Validation: TEST("FullyQualifiedName~XboxInputContextMachineTests"); DET. Acceptance: A does not fire while any menu/overlay is open; entering a menu emits a one-shot neutral push; Resolve deterministic; DET == baseline.

- S12. Remappable bindings + source-gen persistence + single deadzone/config flow (R9). onConsole=false. Dependencies: S10, S11. Tests-first: TEST-SYSBTN-003 (edited BindingProfile round-trips through BindingJsonContext + in-memory IBindingStore; ResetToDefaults yields Default; remap changes the produced AppCommand; no-reflection guard references only BindingJsonContext.Default) + a single-config-resolution test (deadzone read from ONE resolved XboxInputConfig sourced from INI [ViceSharpXbox]; bindings.v1.json referenced from the INI; no two competing stores feed the converter). RED: IBindingStore/BindingJsonContext/remap + resolved-config path absent. GREEN: source-gen persistence + remap + one resolved config implemented; all pass (FR-SYSBTN-008; TR-SYSBTN-003). Validation: TEST("FullyQualifiedName~XboxBindingPersistenceTests"). Acceptance: profile round-trips byte-identical; ResetToDefaults restores Default; one config flow feeds the converter.

- S13. AppCommand -> host-command dispatcher seam (marshaled; fakes). onConsole=false. Dependencies: S3, S11. Tests-first: TEST-SYSBTN-002 (each AppCommand invokes the correct recorded host call on fake IEmulatorHost/ISnapshotService/ISettingsService; AutostartDrive8->ResetAndAutostartDrive8Async, Warm/ColdReset->Warm/ColdResetAsync, QuickSave/Load->Capture/RestoreSnapshotAsync, Warp->UpdateSettings limiter toggle, Swap->config flag only with no settings call; RequestExit only via ConfirmYes in ConfirmDialog; state-mutating commands routed through the session-lock sink, not the input thread). RED: AppCommandDispatcher/IHostCommandSink absent. GREEN: dispatcher implemented against the host POCO interfaces (FR-SYSBTN-004/006/007; TR-SYSBTN-004). Validation: BUILD; TEST("FullyQualifiedName~XboxAppCommandDispatcher"). Acceptance: each command maps to exactly one correct host call; Swap issues no ISettingsService call; Exit requires ConfirmYes.

- S14. XboxDataPathBridge + keymap seeding + startup ordering + AppContainer keymap write-path (R9). onConsole=false. Dependencies: S3. Tests-first: XboxDataPathBridgeTests (Configure sets VICESHARP_ROM_PATH + creates <root>\C64 so ViceDataPathResolver.FindDataRoots() contains <root>; copies packaged *.vkm into C64Path when absent; returns configDir usable by ViceSettings.OpenAt; the keymap-write path lands in the writable LocalFolder root, not a temp file). RED: bridge absent; resolver returns empty for the temp root. GREEN: with ILocalDataFolder at a temp dir, Configure does all of the above; ViceDataPathResolver.cs and InputServiceHost.cs unchanged except the write-path redirect (FR-XROM-003; TR-XPATH-001). Validation: TEST("FullyQualifiedName~Xbox.XboxDataPathBridgeTests"). Acceptance: env var set before the resolver call; <root>\C64 created; FindDataRoots contains <root>; vkm copied only when absent; keymap write path is the AppContainer-writable root.

- S15. SettingsOptionCatalog in Protocol + desktop parity. onConsole=false. Dependencies: S0. Tests-first: SettingsOptionCatalogTests (every list matches the AttachPanelViewModel tables; To/From round-trip stable; ToPaletteId('Pepto')=='pepto', ToCropModeId('Borderless')=='borderless', ToInputPort('Joystick 1')==Joystick1). RED: SettingsOptionCatalog absent; parity asserts fail. GREEN: the catalog exposes the 10 lists + all To*/From* maps byte-identical to desktop (TR-XSET-002). Validation: TEST("FullyQualifiedName~Xbox.SettingsOptionCatalogTests"). Acceptance: lists equal desktop; From*(To*(x))==x for every option.

- S16. Pure XAudio2 ring/queue math (ring-buffer decoupling; back-pressure-free) (R3). onConsole=false. Dependencies: S1. Tests-first: TEST-XAUDIO-002 (XAudio2AudioMath QueuedSampleCount/AvailableSampleCount/RoomAvailable/NextFreeBufferIndex across 0..bufferFragmentCount; ring drop-oldest on over-run, brief-silence on under-run; non-blocking semantics). RED: the math type does not exist. GREEN: all boundary cases pass off-console; math mirrors WinMmAudioBackend.ComputeAvailableBytes semantics but decoupled (no blocking) per R3. Validation: TEST("FullyQualifiedName~Xbox.Audio.XAudio2AudioMath"). Acceptance: full ring -> RoomAvailable false + drop-oldest; empty -> under-run silence; index wraps correctly.

- S17. XboxAudioBackendFactory selection + silent-headless guarantee. onConsole=false. Dependencies: S6, S16. Tests-first: TEST-XAUDIO-001 (CreateDefault() returns null when device-disabled/headless; SID built null-backend is inert: IsAudioTimingSource==false, no samples) + TEST-XAUDIO-003 (EffectiveGain applied once via AudioSampleConverter for volume/mute). RED: factory type absent. GREEN: factory returns null headless; non-null wraps in CaptureAudioTap (DefaultEmulatorRuntimeFactory.cs:154-158); null keeps SID inert (SidFactory.cs:37-40) (FR-XAV-001; FR-XAUDIO-002; TR-XAUDIO-001/003). Validation: TEST("FullyQualifiedName~Xbox.Audio.XboxAudioBackendFactory"); DET. Acceptance: headless -> null -> SID inert/deterministic; no audio device opened during the run; DET == baseline.

- S18. XAudio2SourceVoiceBackend interop (AOT-clean vtable; non-blocking ring sink; Pause unparks). onConsole=false. Dependencies: S16, S17, S4 (head to publish AOT). Tests-first: TEST-XAUDIO-004 group: on device-open failure the backend degrades to a silent no-op (SubmitSamples returns, QueuedSampleCount==0); SubmitSamples never blocks the worker (writes to the ring, returns immediately); Pause() releases any waiter and unparks a worker (liveness); blittable WAVEFORMATEX/XAUDIO2_BUFFER + delegate* unmanaged compile under DisableRuntimeMarshalling with 0 AOT warnings. RED: backend type absent. GREEN: backend implements IAudioBackend; degrades silently on CI; non-blocking ring submit; Pause unparks; AOT-analyzer-clean (FR-XAUDIO-003; TR-XAUDIO-001/002). Validation: TEST("FullyQualifiedName~Xbox.Audio.XAudio2SourceVoiceBackend"); `dotnet build src/ViceSharp.Xbox/ViceSharp.Xbox.csproj -c Release -p:ViceSharpXboxUwp=false /p:PublishAot=true` (link-clean where feasible). Acceptance: no winmm/kernel32 imports; only [LibraryImport("xaudio2_9.dll")] XAudio2Create; SubmitSamples non-blocking; Pause unparks the worker.

- S19. ViceSharp.Xbox.ViewModels scaffold + boundary test (R12). onConsole=false. Dependencies: S15 (Protocol catalog), S7. Tests-first: XboxBoundaryTests (project exists; references only Abstractions + Protocol + Xbox.Input; forbidden-identifier scan bans Core/Chips/Architectures/Host/Avalonia/Grpc/XAML, modeled on AvaloniaBoundaryTests.cs:11-73). RED: csproj missing. GREEN: create the net10.0 lib, add to slnx + TestHarness ProjectReference; boundary asserts pass (FR-XBOXUI-007; TR-XBOXUI-007/TR-XMVVM-001). Validation: BUILD; TEST("FullyQualifiedName~XboxBoundaryTests"). Acceptance: references-only-Abstractions+Protocol+Xbox.Input; no runtime/engine/XAML reference.

- S20. Navigation stack + InputContext observer over XboxInputContext. onConsole=false. Dependencies: S19, S11. Tests-first: TEST-XBOXUI-001 (NavigationViewModelTests: push Settings->DeviceSetup, GoBack twice -> Home in order; opening quick menu/virtual keyboard pushes no stack entry) + TEST-XBOXUI-003 (InputContext observer: quick menu -> non-Gameplay; closing with empty stack -> Gameplay; navigation requests XboxInputContext.RequestContext). RED: NavigationViewModel/observer absent. GREEN: explicit stack + overlay flags + observer reflecting the single XboxInputContext authority (FR-XBOXUI-001/003; TR-XBOXUI-001/003). Validation: TEST("FullyQualifiedName~NavigationViewModelTests|FullyQualifiedName~InputContextObserver"). Acceptance: stack correctness; exactly one context authoritative.

- S21. Host-facade seam (IEmulatorSessionFacade/ILocalVideoFramePull/IXboxSettingsGateway) + fakes + round-trip. onConsole=false. Dependencies: S19, S3. Tests-first: HostingSeamTests (round-trip against FakeEmulatorSessionFacade/FakeVideoFramePull/FakeXboxSettingsGateway: create->start->pause->resume->reset; GetJoystickInput/GetKeyboardInput; ILocalVideoFramePull.TryCopyFrameInto mirrors LocalVideoFrameSource). RED: seam + fakes absent. GREEN: seam interfaces owned by the ViewModels, fakes drive round-trips (FR-XBOXUI-004 support; TR-XBOXUI-004). Validation: TEST("FullyQualifiedName~HostingSeam"). Acceptance: round-trip green against fakes; the head's real adapter (over IConsoleEmulatorHost) is validated on-console.

- S22. Focus graph builder (FocusMapBuilder). onConsole=false. Dependencies: S19. Tests-first: TEST-XBOXUI-002a (FocusMapBuilderTests: BuildGrid(3,2) edges correct, top-edge Up null, no wrap). RED: builder absent. GREEN: pure grid focus math (FR-XBOXUI-002; TR-XBOXUI-002). Validation: TEST("FullyQualifiedName~FocusMapBuilder"). Acceptance: edges correct, no wrap.

- S23. Video frame-pull adapter (pure sink) + zero-alloc gate + FrameGeometry (R10/R13). onConsole=false. Dependencies: S19, S3. Tests-first: TEST-XBOXUI-005a (cadence: 10 fires/200ms -> 10 pulls +/-1) + TEST-XBOXUI-005b (VideoSurfacePureSinkBoundaryTests forbidden-identifier scan: no RunFrame/StepInstruction/Reset/SetJoystickState/SetKeyState) + TEST-XVIDEO-001 (byte-exact 384x272 BGRA copy + no-advance spy, sized from FrameGeometry.BufferLength) + TEST-XVIDEO-002 (GC.GetAllocatedBytes delta==0 over the steady-state render tick). RED: VideoFramePullViewModel absent. GREEN: pure pull adapter reads only TryCopyFrameInto into a pinned FrameGeometry-sized buffer, zero-alloc, never advances the core (FR-XBOXUI-005; FR-XVIDEO-002; TR-XBOXUI-005/TR-XVIDEO-001). Validation: TEST("FullyQualifiedName~VideoFramePull|FullyQualifiedName~VideoSurfacePureSink|FullyQualifiedName~Xbox.Video.C64FramePump"); DET. Acceptance: cadence within +/-1; pure-sink scan passes; zero-alloc gate green; DET == baseline.

- S24. RESTORE / NMI seam (FR-XKBD) in Abstractions + Host.InProcess facade. onConsole=false. Dependencies: S3. Tests-first: TEST-XKBD-001 (facade.SetRestoreState routes to the C64 NMI path keycode 0x31 distinct from SetKeyState, via a spy IMachineKeyboardInput; a SetKeyState("*") does NOT fire RESTORE). RED: no SetRestoreState seam. GREEN: IMachineKeyboardInput.SetRestoreState(bool) added to Abstractions + IConsoleEmulatorHost.SetRestoreState, routed to the NMI trigger (FR-XKBD-001; TR-XKBD-001). Validation: TEST("FullyQualifiedName~Xbox.Restore|FullyQualifiedName~XKbd"); DET. Acceptance: RESTORE reaches the NMI path only via the dedicated seam; DET == baseline.

- S25. Virtual keyboard layout + emission + RESTORE tile drives FR-XKBD. onConsole=false. Dependencies: S22, S24. Tests-first: TEST-XBOXUI-006 (RETURN tile emits SetKeyState("Return",true) then false; shift-latch + F1 emits "F2"; every KeyName in the known-good C64KeyboardMap alias list; RESTORE tile calls SetRestoreState, not SetKeyState). RED: VirtualKeyboardViewModel absent. GREEN: layout + emission + shift-latch + RESTORE-tile wiring (FR-XBOXUI-006; FR-XKBD-001; TR-XBOXUI-006). Validation: TEST("FullyQualifiedName~VirtualKeyboardViewModelTests"). Acceptance: exact key strings; shift-latch swaps F1->F2; RESTORE drives the NMI seam.

- S26. XboxSettingsViewModel bind/apply/validate/revert (against the fake gateway) (R11 split a). onConsole=false. Dependencies: S15, S21. Tests-first: XboxSettingsViewModelTests (Refresh loads host settings/profiles; Apply sends canonical DTO ids + clears dirty; adopts host-canonical returned settings; profile/resource-mode -> RequiresRestart; Validate does not apply + populates results; Revert restores last-applied). RED: VM + fake gateway absent. GREEN: VM reproduces desktop settings behaviors (FR-XSET-001/002/003/004; TR-XSET-002/TR-XMVVM-001). Validation: TEST("FullyQualifiedName~Xbox.XboxSettingsViewModelTests"). Acceptance: canonical-id apply + host-canonical adoption + restart gating + non-applying validate + full revert.

- S27. XboxDeviceSetupViewModel attach/eject + true-drive + drive-model (R11 split a). onConsole=false. Dependencies: S21. Tests-first: XboxDeviceSetupViewModelTests (slots have expected kinds/patterns; Attach reads payload + calls host, Eject detaches; drive-model change on active true-drive rebuilds with (int)model 1542 for 1541-II; inactive-slot change no host call; enabling second true-drive disables first, single rig). RED: VMs + fakes absent. GREEN: device VMs (FR-XDEV-001/002/003; TR-XMVVM-001). Validation: TEST("FullyQualifiedName~Xbox.XboxDeviceSetupViewModelTests"). Acceptance: AvailableDriveModels==DriveModelCatalog.Implemented; 1542 flows to SetTrueDriveAsync; single-rig invariant.

- S28. RomProvisionEvaluator + XboxRomProvisioningViewModel (first-run provisioning) (R11 split b). onConsole=false. Dependencies: S14, S21. Tests-first: RomProvisionEvaluatorTests (NoFiles->NotProvisioned; WrongBytes->Invalid; PartialPresence->Partial; UltimaxProfile->KernalOptional) + XboxRomProvisioningViewModelTests (Import validates size+MD5, rejects oversize/mismatch without mutating state; Download requires confirm then transitions state; IsBootBlocked true until Complete). RED: evaluator/VM + fake IRomAcquirer/IStoragePicker absent. GREEN: evaluator computes state from presence+size+hash; VM enforces the 64MB ceiling + confirm-gated download + IsBootBlocked (FR-XROM-001/002; TR-XPATH-001). Validation: TEST("FullyQualifiedName~Xbox.RomProvisionEvaluatorTests|FullyQualifiedName~Xbox.XboxRomProvisioningViewModelTests"). Acceptance: states per profile; oversize/mismatch rejected state-unchanged; download only after confirm; IsBootBlocked==false only when Complete (or kernal not required).

- S29. XboxUiStateStore persistence to vice-sharp.ini. onConsole=false. Dependencies: S14. Tests-first: XboxUiStateStoreTests (Save then Load round-trips all 8 prefs; writes only [ViceSharpXbox] leaving vice.ini untouched; missing file -> defaults). RED: store absent. GREEN: store persists via ViceSettings.SetViceSharp/Save into [ViceSharpXbox] in a temp dir and reloads identical values (FR-XSET-005; TR-XPERSIST-001). Validation: TEST("FullyQualifiedName~Xbox.XboxUiStateStoreTests"). Acceptance: round-trip equality; only vice-sharp.ini changes; defaults when absent.

- S30. Home/InputMapping/About page ViewModels (About GPL disclosure). onConsole=false. Dependencies: S19. Tests-first: TEST-XBOXUI-008 (AboutViewModel: LicenseIdentifier=="GPL-2.0-or-later", AttributionText contains "VICE", SourceOfferText non-empty + URL) + Home/InputMapping bindability. RED: page VMs absent. GREEN: page VMs with fixed disclosure strings (FR-XBOXUI-008; TR-XBOXUI-008). Validation: TEST("FullyQualifiedName~AboutViewModel|FullyQualifiedName~HomeViewModel|FullyQualifiedName~InputMappingViewModel"). Acceptance: About discloses GPL/VICE/source URL.

- S31. Package.appxmanifest (Windows.Xbox + capability rule) validated off-console. onConsole=false. Dependencies: S4. Tests-first: TEST-XBOXPKG-003 (Manifest parses; targets Windows.Xbox Min 10.0.19041.0 MaxTested 10.0.26100.0; x64; declares internetClient iff the download path is kept, else no restricted capability; manifest ItemGroup head-only guarded so the fallback build ignores it). RED: manifest absent. GREEN: manifest exists + parses + Windows.Xbox + capability rule; inert on the net10.0 fallback (FR-XBOXPKG-003; TR-XBOXPKG-002). Validation: TEST("FullyQualifiedName~XboxManifest"); BUILD. Acceptance: manifest asserts Xbox device family + capability rule; slnx build unaffected.

- S32. GPL compliance: license + vkm Content + THIRD_PARTY_NOTICES + no-ROM payload (R8). onConsole=false. Dependencies: S31. Tests-first: TEST-XBOXGPL-001 (staging includes COPYING + THIRD_PARTY_NOTICES.md under Licenses/ with vkm GPL attribution; *.vkm present under Assets\vice-data\C64; payload contains no kernal-*/basic-*/chargen-*.bin; a compiled SourceUrl constant exists). RED: license/vkm/ROM-exclusion/source-URL not staged. GREEN: head-only Content copies license + vkm; no-ROM payload allows vkm; SourceUrl compiled in (FR-XBOXGPL-006; TR-XBOXCI-005). Validation: TEST("FullyQualifiedName~XboxGpl"); `pwsh ./build.ps1 ValidateXbox`. Acceptance: ROM-free payload with vkm allowed; license notices + source URL present.

- S33. Nuke targets (PublishXbox/DeployXbox/ValidateXbox) + CI-exclusion + rollback doc. onConsole=false. Dependencies: S32. Tests-first: XboxNukeConfigTests (Build.cs declares the three targets with params/paths) + XboxCiConfigTests (azure-pipelines.ci.yml/release.yml name no PublishXbox/DeployXbox) + a rollback-doc presence assertion. RED: targets/CI-exclusion/rollback doc absent. GREEN: targets defined; device targets absent from both CI arrays; ValidateXbox runs off-console; rollback step (WinAppDeployCmd uninstall + prior-MSIX redeploy) documented (TR-XBOXCI-005; R8 rollback). Validation: TEST("FullyQualifiedName~XboxNukeConfig|FullyQualifiedName~XboxCiConfig"); `pwsh ./build.ps1 ValidateXbox`. Acceptance: ValidateXbox exit 0 on a workload-less agent; existing CI/release jobs unchanged; rollback documented.

- S34. UWP XAML head build: pages, controls, Win2D video sink, XAudio2 wiring, WinRT gamepad source, App composition + adapter, focus visuals. onConsole=false (workload-dependent build). Dependencies: S18, S20, S21, S22, S23, S25, S26, S27, S28, S29, S30, S31. Tests-first: XboxUwpHeadShellTests (raw-file-read: TFM net10.0-windows10.0.26100.0, UseUwp, PublishAot(Release), DisableRuntimeMarshalling, TargetDeviceFamily Windows.Xbox, RequiresPointer=Never; references ViewModels/Xbox.Input/Abstractions/Core/Chips/Architectures/Host.InProcess and NOT Host/Avalonia/Protocol-Grpc/Monitor). RED: head XAML/App/adapter absent. GREEN: create six Pages + VideoSurfaceHost (Win2D SwapChainPanel) + VirtualKeyboardOverlay + QuickMenuOverlay + FocusVisuals.xaml + App.xaml.cs composition + the adapter implementing the ViewModels seams over IConsoleEmulatorHost + WinRT gamepad source + XAudio2 wiring; `dotnet build src/ViceSharp.Xbox -p:ViceSharpXboxUwp=true` on a workload-equipped machine/CI. Validation: TEST("FullyQualifiedName~XboxUwpHeadShell"); `dotnet build src/ViceSharp.Xbox/ViceSharp.Xbox.csproj -c Release -p:Platform=x64 -p:ViceSharpXboxUwp=true` (workload machine). Acceptance: shell test green off-console; XAML head builds on the workload machine.

- S35. Desktop-intact + full-suite + AOT regression guard. onConsole=false. Dependencies: all off-console slices to date. Tests-first: TEST-TESTGATE-009 DesktopRegressionGuard (slnx Release build; Category=Determinism 0 failed at baseline count; AvaloniaBoundaryTests green) + TEST-XBOXAOT-001 re-run + XboxTestConventionTests (no ViceFact/Assert.Skip in Category=Xbox scope). RED: a prior slice regressed the desktop build/determinism/boundary or reintroduced a trim/AOT warning. GREEN: all guards green (FR-TESTGATE-002/003; TR-XBOXTOPO-003). Validation: BUILD; DET; TEST("FullyQualifiedName~Avalonia"); AOT; TEST("FullyQualifiedName~XboxTestConvention"). Acceptance: Determinism pass-count == baseline; AvaloniaBoundaryTests green; AOT 0 warnings; Category=Xbox Skipped=0.

- S36. On-console deploy + boot-to-menu + C64 render smoke. onConsole=true. Dependencies: S0, S34. Tests-first: on-console checklist (deploy Release MSIX via Dev Mode; boot to the 10-foot menu with XY focus; render a live C64 frame). RED: MSIX fails to deploy or does not reach the menu / render. GREEN: app sideloads, boots to menu with controller focus, renders a live C64 frame (FR-XBOXUI-001/002; FR-XVIDEO-001). Validation: on-console deploy + checklist. Acceptance: MSIX deploys clean; menu focus-navigable; C64 frame visible.

- S37. On-console live gamepad smoke (JOY2/JOY1 + A/B fire + menu neutralization + swap). onConsole=true. Dependencies: S36. Tests-first: input-telemetry checklist (L stick+DPad drive JOY2, R stick JOY1, A/B fire respective ports; menu open forces neutral + A does not fire; L3 swaps; Guide does not affect the app; disconnect centers both ports). RED: mapping/adapter defect. GREEN: all directions/fire/gating/swap confirmed via the overlay (TEST-GAMEPAD-002; TEST-SYSBTN-DEVICE; FR-CTX-002). Validation: on-console overlay checklist. Acceptance: 8 directions per port; A->JOY2 fire, B->JOY1 fire; menu neutralization; Guide inert.

- S38. On-console A/V smoke (Win2D video + XAudio2 audio + mute + suspend/resume). onConsole=true. Dependencies: S36. Tests-first: A/V checklist (boot C64: frame-on-TV letterboxed correct colors no tear; audible SID; mute toggles silence; volume scales; Suspend/Resume does not deadlock the worker or corrupt state). RED: no audio (AppContainer path broken) or video stutter/black. GREEN: continuous Win2D video + audible XAudio2 audio, mute/volume honored, clean suspend/resume (FR-XVIDEO-001; FR-XAUDIO-001/002/003). Validation: on-console checklist + capture receipts. Acceptance: image correct; SID audible; mute silences; suspend/resume clean.

- S39. On-console system-button + context smoke. onConsole=true. Dependencies: S36. Tests-first: checklist (Menu opens/closes menu; View toggles virtual keyboard; X autostarts drive 8; Y warm-reset behind confirm; LT warps while held; L3 swaps sticks; A fires JOY2 only in gameplay, never in a menu; RESTORE tile fires the NMI). RED: buttons do nothing / conversion untested / RESTORE unwired. GREEN: end-to-end button + context behavior on hardware (TEST-SYSBTN-DEVICE; FR-XKBD-001). Validation: on-console checklist. Acceptance: all system-button behaviors + RESTORE confirmed on device.

- S40. On-console first-run ROM provisioning + settings persistence + device attach E2E (R11 split c). onConsole=true. Dependencies: S36. Tests-first: checklist (first-run wizard acquires the 3 core ROMs (download or USB) into LocalFolder\vice\C64; boot reaches READY; a palette/scale change survives an app relaunch (vice.ini + [ViceSharpXbox]); attach a d64 with a working 1541/1540/1541-II selector; all controller-only within the TV-safe area). RED: no Settings/Device UI or boot blocked with no way to provision. GREEN: provisioning + persistence + attach E2E on device (FR-XROM-001/002/003; FR-XSET-001/005; FR-XDEV-001/002). Validation: on-console checklist. Acceptance: ROMs provisioned + boot to READY; setting persists across relaunch; d64 attach with drive-model selector; controller-only XY nav.

- S41. On-console performance within resource budget (PAL 50). onConsole=true. Dependencies: S36. Tests-first: perf checklist (sustained target fps over a fixed workload within ~1GB RAM / 2-4 shared cores / 45% GPU headroom). RED: exceeds RAM/cores or drops below target fps. GREEN: sustained region-appropriate fps within budget (default PAL 50; see operator-confirm item 2). Validation: on-console fixed-workload benchmark + Dev Mode tooling capture. Acceptance: sustained fps; RAM under ~1GB; no CPU-starvation/thermal throttle.

Slice-count summary: 42 slices S0..S41; 7 are onConsole=true (S0 feasibility go/no-go, S36-S41 device validation); the other 35 are off-console gated on plain net10.0. On-console work is isolated behind S0 and the off-console deps.

## 7. Risks and mitigations (merged, deduped)

- THE unproven unknown: no Microsoft confirmation that a modern-.NET Native-AOT UWP MSIX (with Win2D) deploys and runs on the physical Xbox console and that the trimmed core+Host.InProcess graph links clean. Mitigation: single S0 go/no-go retires it before any production investment; documented AOT->JIT and Win2D->DX11 fallbacks.
- Windows App SDK / UWP workload absent on CI and most dev boxes. Mitigation: the `ViceSharpXboxUwp=false` net10.0 fallback keeps `dotnet build ViceSharp.slnx` green everywhere; S34 XAML build + all device slices run only on the operator's equipped machine/console; the harness references the portable libs, never the head.
- Extraction blast radius (S1 moves ~15+ files). Mitigation: preserve namespaces + assembly-internal visibility (add `InternalsVisibleTo` for the harness); existing Host tests re-run from the new location as guards; the split follows the existing Grpc-wraps-POCO seam.
- AOT/trim forbids runtime reflection; a stray `ConfigurationBinder.Get<T>`, YamlDotNet reflective path, or shipped ViceNative/WinMm call would emit IL2xxx/IL3xxx (fatal under TreatWarningsAsErrors). Mitigation: S5/S35 AOT publish gate + the reflection-free audit; core kept as-is (already IsAotCompatible); DisableRuntimeMarshalling on the head.
- XAudio2 COM under Native AOT + DisableRuntimeMarshalling is non-trivial. Mitigation: isolate in blittable vtable interop (`XAudio2Interop`), off-console degrade test (S18), AOT-analyzer-clean build; xaudio2_9.dll name validated at S0/device.
- Win2D packaging on the locked-down Xbox target (per-arch native binary, rd.xml/trimming). Mitigation: S0 adds the Win2D+AOT packaging check; DX11 interop is the S0-authorized fallback only.
- Determinism regression from the pacing-gate swap or the audio ring. Mitigation: single wall-clock gate never touches per-step cycle counts; DET re-run at S1/S2/S3/S11/S17/S23/S24/S35 with pass-count == baseline; determinism tests bypass the gate via StepFramesDeterministic.
- Live input nondeterminism vs the bit-exact-replay invariant. Mitigation: live-gamepad replay scoped OUT for the console head (R10, operator-confirm 1); the core stays deterministic (input pushed via the identical SetJoystickState seam); lockstep-vs-x64sc remains a desktop concern with scripted input.
- Double-pathing / double-reading input (menu neutralization, app-button consumption). Mitigation: one XboxInputContext single-consumer owner (R5); no separate AppButtonChanged path.
- WinRT GamepadButtons bit drift or thumbstick-Y sign assumption. Mitigation: S7 pins the WinRT-identical bit values with an assert; S37 device smoke is the authoritative sign check and can flip a single bridge constant.
- Worker starvation on a 2-4 core AppContainer partition. Mitigation: unpinned AboveNormal worker; the managed gate spawns no aux timer thread; audio drains on the XAudio2 callback, not the worker.
- MasterAudioControl relocation changes assembly identity (reflection/string lookups on the old namespace would break). Mitigation: the location test + solution build guard; relocation sequenced (S6) before ViewModels bind (S26) and before the XAudio2 backend (S17/S18).
- GPL vs Microsoft Store section-6 conflict; ROM copyright. Mitigation: MVP is Dev-Mode sideload + GitHub source-with-release; Store deferred behind a documented legal review; ROM-free payload enforced by test; vkm GPL data packaged with attribution.
- Frame-tick cadence (called more/less than once per frame). Mitigation: the in-process host owns the single call site at FrameCompleted; delta emission makes the pump idempotent per identical snapshot.
- ApplicationData.Current.LocalFolder semantics under AOT UWP on console only fully confirmed on device. Mitigation: isolated in XboxDataPathBridge; S40 device slice confirms; resolver stays unchanged and env-bridged.
- WinAppDeployCmd/Device Portal PIN pairing drift across Xbox OS updates. Mitigation: DeployXbox documents the Device Portal Add-app fallback + the uninstall/rollback path.

## 8. Rollout, deploy, rollback, CI coexistence, GPL

Deploy (Dev-Mode sideload): build the AOT MSIX on a workload-equipped Windows machine (`pwsh ./build.ps1 PublishXbox` -> `artifacts/xbox/ViceSharp.Xbox.msix`), pair the console via Device Portal PIN, and install with `WinAppDeployCmd install -file <msix> -ip <console-ip> -pin <pin>` (or the Device Portal / Dev Home "Add app" fallback). Debug JIT MSIX (`-c Debug -p:PublishAot=false --self-contained true`) is the fast inner-loop path; Release AOT MSIX is the Store/GitHub path. Build/run x64 (Xbox Dev-Mode partition is x64-only + DX11); Visual Studio "Remote Machine" x64 deploy is the equivalent IDE path.

Rollback / uninstall (R8): to revert a bad sideload, `WinAppDeployCmd uninstall -package <pkgfullname> -ip <ip> -pin <pin>` (or Device Portal remove), then redeploy the prior known-good MSIX (`WinAppDeployCmd update`/`install`). This is documented in `src/ViceSharp.Xbox/README.md` and asserted present by S33. The desktop build is unaffected by any console deploy (additive design).

CI coexistence: workload-absent agents build the whole solution via the `ViceSharpXboxUwp=false` net10.0 fallback; the head compiles as a plain net10.0 assembly with a trivial entry, no XAML/manifest/MSIX. The Nuke `PublishXbox`/`DeployXbox` device targets are deliberately absent from both `DefaultPoolAzurePipelines` `InvokedTargets` arrays (`Build.cs:26,36-43`), so `azure-pipelines.ci.yml`/`azure-pipelines.release.yml` never try to build the head or touch a console; `ValidateXbox` is the CI-safe off-console check (fallback build + manifest XML validate + CI-array-exclusion assert). Every gate re-runs `Category=Determinism` and requires the pass-count to equal the pre-S1 baseline; `Category=Xbox` is authored skip-free so 0-skipped is genuine (`XboxTestConventionTests`).

GPL-2.0-or-later source availability: the public repo satisfies section-3 corresponding-source; additionally the MSIX bundles `COPYING` + `THIRD_PARTY_NOTICES.md`, the About screen surfaces the compiled `SourceUrl`, and each GitHub release attaches the MSIX plus a `git archive` source tarball of the tagged commit. The shipped GPL VICE `*.vkm` keymap data is packaged as Content under `Assets\vice-data\C64\` with explicit GPL attribution in `THIRD_PARTY_NOTICES.md` (confirm vkm redistributability). ROMs are never bundled (Commodore copyright); they are user-provided (USB import) or optionally fetched over verified HTTPS (requires `internetClient`). Microsoft Store submission is deferred pending a documented GPL section-6 (no-further-restrictions) review vs the Store Standard Application License Terms.

## 9. Operator-confirm items (defaults chosen; one-line yes/no each)

1. Scope live-gamepad deterministic bit-exact REPLAY OUT for the console head (core stays deterministic via the SetJoystickState seam; lockstep-vs-x64sc stays a desktop/scripted concern)? Proposed default: yes, out of scope.
2. Region/fps target for the S41 perf budget = PAL 50 fps (VICE x64sc default)? Proposed default: PAL 50.
3. ROM acquisition: USB/local import as the primary path, plus an OPTIONAL verified-HTTPS download that requires the `internetClient` capability? Proposed default: yes, USB primary + optional HTTPS download with `internetClient`.
4. Freeze the exact default deadzone + hysteresis literals (RadialDeadzone 0.30, ActivateThreshold 0.55, ReleaseThreshold 0.40) before the S8/S9 golden vectors? Proposed default: these values.
5. Add the dedicated RESTORE/NMI seam (`IMachineKeyboardInput.SetRestoreState(bool)` + facade `SetRestoreState`) under FR-XKBD so the virtual keyboard's RESTORE tile works, rather than shipping the tile disabled? Proposed default: yes, add the seam.
6. Who installs the windows-app (UWP) workload and on which machine/CI runner for S34 (XAML build) and the device slices, and what is the MSIX signing approach for sideload (self-signed dev cert vs Partner Center-issued)? Proposed default: operator's equipped Windows box + a self-signed dev cert trusted on the console for MVP.

## 10. Residual open items still needing cross-team reconciliation

- Xbox.ViewModels reference surface: R12/tenfoot said "Abstractions-only", but the settings gateway uses Protocol DTOs and `SettingsOptionCatalog` lives in Protocol, so this plan permits `ViceSharp.Xbox.ViewModels` to reference Abstractions + Protocol (+ Xbox.Input). Both are portable, Kestrel-free, AOT-safe, but this is a deliberate refinement of R12 that should be ratified (alternative: split the settings/device/ROM VMs into a Protocol-referencing sub-lib and keep the pure UI VMs Abstractions-only).
- GamepadButtonFlags bit layout: R4's operative rule ("WinRT-identical, cast with no per-bit remap") selects the 0x40-DPadUp layout used here, which contradicts R4's parenthetical attribution to "the system-buttons section's layout" (1<<10). Confirm the WinRT-identical layout is intended (it is the only one the cast-from-WinRT step can use without a remap).
- XboxJoystickMapper hysteresis threading: R5's exposed signature `Map(in GamepadSnapshot, in XboxInputConfig) -> (JoystickPortState, JoystickPortState)` omits explicit latch state; this plan threads the hysteresis `ConverterState` through the single-owner XboxInputContext to keep the mapper pure and FR-GAMEPAD-004 satisfiable. Confirm this internal threading is acceptable (alternative: fold the latch into an explicit prior/next overload on the public contract).
- Confirm the exact modern-UWP AOT publish property set the installed .NET 10 / VS 2026 UWP template emits (`UseUwp` + `EnableMsixTooling` vs the windows-app project SDK; `WindowsPackageType` vs `AppxPackage`; `win-x64` vs `win10-x64`) on the operator's equipped machine before finalizing the S4/S31/S34 csproj+manifest text.
- Confirm `TargetPlatformMinVersion`/`MinVersion` for the oldest supported Xbox One OS (10.0.19041.0 assumed) vs a higher floor if AOT requires it.
- Confirm vkm redistributability under GPL-2.0-or-later and the exact set of shipped `*.vkm` files + their `THIRD_PARTY_NOTICES.md` placement.
- Quick-save/quick-load slot policy (single rolling LocalFolder slot for LB/RB vs named slots) with the storage/settings subsystem; this plan assumes a single slot.
- Persistence across UWP suspend/terminate for Settings/Device Setup state (the tenfoot section flagged this may need its own FR/TR/TEST); covered in spirit by `XboxUiStateStore` + plain-C# ViewModels surviving suspend, but confirm no additional session-restore requirement is needed.
- Consolidation follow-up: `SettingsOptionCatalog` is lifted (copied) into Protocol to avoid touching Avalonia now; a later change should point `AttachPanelViewModel` at it to prevent drift (out of scope here to keep the desktop intact).
- Multi-controller twin-port assignment (controller A->JOY2, controller B->JOY1) is modeled but not enabled by the default single-pad twin-stick profile; a future FR can enable it without core changes.


