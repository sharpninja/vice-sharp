# AI Code Review

- **Captured (UTC):** 20260622T135955866Z
- **Review kind:** code
- **Status:** fail
- **Summary:** Multiple correctness, concurrency and resource-lifetime defects in media capture surface: SyncRoot held across long joins in Stop, single audio tap not arbitrated for mixed capture kinds, enqueue back-pressure blocks while holding emulation locks, shutdown races on stream close after timeout, listeners left listening after accept.

## Prompt

```text
MANDATORY FIRST STEP: read and fully follow AGENTS-README-FIRST.yaml at the repository root before reviewing - its session-start, signature/nonce verification, authentication, and MCP Server usage instructions all apply to this review. The MCP Server described in that marker is the SINGLE SOURCE OF TRUTH for requirements: pull the authoritative functional, technical, and test requirements (FR/TR/TEST) from the MCP Server requirements API (the /mcpserver/requirements/fr, /tr, and /test endpoints) and assess requirements coverage and traceability ONLY against those MCP records. FR-to-TR-to-TEST traceability is recorded in the /mcpserver/requirements/mapping endpoint (entries shaped {frId, trIds, testIds}), NOT as inline linkedFr/linkedTr fields on a TEST record; read that mapping endpoint to assess coverage, and do NOT report a TEST record as untraceable merely because it has no inline link fields - that is the expected schema, not a defect. Do NOT treat any requirements artifact outside the MCP Server as authoritative - this includes docs/Project/Functional-Requirements.md, docs/Project/Technical-Requirements.md, docs/Project/Testing-Requirements.md, docs/Project/Requirements-Matrix.md, docs/Project/TR-per-FR-Mapping.md, and any GitHub/Azure DevOps wiki export. Do NOT raise findings about coverage gaps, counts, or drift in those out-of-band documents; they are non-authoritative mirrors of the MCP Server. For Grok Build, the workflow.requirements.* names are plugin shim methods, not literal search_tool results. If dedicated requirements tools are not visible but pwsh is available, use the Grok plugin helper exactly as supported by mcpserver-grok-plugin: set GROK_PLUGIN_ROOT from the process environment or F:\GitHub\mcpserver-grok-plugin, set PLUGIN_AGENT_NAME=GrokCode, then call pwsh.exe -NoProfile -NonInteractive -File "$env:GROK_PLUGIN_ROOT\lib\repl-invoke.ps1" -Method workflow.requirements.listFr -ParamsYaml "workspacePath: F:\GitHub\vice-sharp"; repeat with workflow.requirements.listTr, workflow.requirements.listTest, and workflow.requirements.listMappings. This is the supported Grok plugin path, not a raw REST or generic helper substitute. RUNTIME ERROR CONTRACT: if authentication, subscription, quota, token/context limit, MCP access, tool permission, or any other runtime problem prevents completing the review, do not pause, wait for input, retry indefinitely, or return prose. Immediately return the same raw aiunit.review.findings.v1 JSON object with status:"error" and a single finding that explains the blocker and exact evidence. OUTPUT CONTRACT: return ONLY the raw aiunit.review.findings.v1 JSON object as your entire response - no prose preamble, no explanation, and no markdown code fences. When status is pass, findings must contain only actionable code defects in the requested review scope; do not include informational, process-only, MCP bootstrap, or trust-status notes as findings. If there are no actionable scoped code defects, return an empty findings array. Perform a code-quality, correctness, and security review of the ViceSharp media-capture surface: the capture host and recorders under src/ViceSharp.Core/Media (FfmpegVideoRecorder, FfmpegVideoEncoding, FfmpegLocator, CaptureAudioTap, WavAudioRecorder, FrameSequenceCapture, RecordingAudioBackend), src/ViceSharp.Host/Services/CaptureServiceHost.cs, and the capture wiring in src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs and src/ViceSharp.Host/Runtime/DefaultEmulatorRuntimeFactory.cs. Focus on concurrency around the emulation worker (frame/audio tee points, the TCP sockets feeding ffmpeg, lock ordering), resource lifetime (process/socket/stream disposal), and input validation. Report findings only.
```

## Response

```json
{
  "schemaVersion": "aiunit.review.findings.v1",
  "reviewType": "code",
  "status": "fail",
  "summary": "Multiple correctness, concurrency and resource-lifetime defects in media capture surface: SyncRoot held across long joins in Stop, single audio tap not arbitrated for mixed capture kinds, enqueue back-pressure blocks while holding emulation locks, shutdown races on stream close after timeout, listeners left listening after accept.",
  "reviewedScope": "src/ViceSharp.Core/Media/* (FfmpegVideoRecorder, FfmpegVideoEncoding, FfmpegLocator, CaptureAudioTap, WavAudioRecorder, FrameSequenceCapture, RecordingAudioBackend, BackgroundByteWriter), src/ViceSharp.Host/Services/CaptureServiceHost.cs, src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs, src/ViceSharp.Host/Runtime/DefaultEmulatorRuntimeFactory.cs",
  "agent": {
    "name": "Grok",
    "provider": "xAI",
    "model": "Grok 4.3"
  },
  "findings": [
    {
      "severity": "critical",
      "category": "concurrency",
      "title": "SyncRoot held across blocking CompleteAndJoin during StopCapture",
      "detail": "StopCaptureAsync (and Close path) acquires session.SyncRoot then calls EndVideoCapture/EndAudioCapture. Those perform recorder.Dispose/Stop which call CompleteAndJoin (ShutdownTimeout 30s for ffmpeg, 10s for WAV). EmulationPumpService acquires the same SyncRoot for AdvanceCleanly \u002B machine.Step \u002B CommitFrame. Capture finalization therefore blocks the emulation worker and all other SyncRoot users for the duration of the join.",
      "recommendation": "Snapshot the active sink/recorder under SyncRoot (and _captureSync), release the lock immediately, then perform CompleteAndJoin \u002B CloseQuietly \u002B ffmpeg WaitForExit/Kill outside any session lock, mirroring the documented pattern used for Start().",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 270,
      "ruleId": "CAPTURE-STOP-HOLDS-LOCK",
      "confidence": 0.95,
      "agent": "Grok"
    },
    {
      "severity": "high",
      "category": "concurrency",
      "title": "CaptureAudioTap admits only one recorder; mixed muxed-video \u002B WAV audio corrupts feeds",
      "detail": "CaptureAudioTap.AttachRecorder simply replaces _recorder. CaptureServiceHost.StartCapture allows independent Video (ffmpeg with includeAudio) and Audio (wav) without cross-check. BeginVideoCapture and BeginAudioCapture both call AttachRecorder. End paths unconditionally Detach. One or both recorders receive zero or wrong samples; muxed container may be truncated or silent.",
      "recommendation": "Add arbitration (e.g. active audio owner kind or ref-count) in EmulatorRuntimeSession/CaptureServiceHost; reject second audio-using capture or refuse Start when tap already feeding a muxed recorder. Make detach kind-aware.",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 197,
      "ruleId": "CAPTURE-AUDIO-TAP-SINGLE",
      "confidence": 0.9,
      "agent": "Grok"
    },
    {
      "severity": "high",
      "category": "concurrency",
      "title": "Enqueue back-pressure blocks emulation worker while holding SyncRoot/_captureSync",
      "detail": "Emulation worker (inside pump SyncRoot lock and for video inside _captureSync) calls sink.CaptureFrame / recorder.WriteSamples which, after brief internal lock, call writer.Enqueue. On full BlockingCollection this blocks the caller. Result: slow sink (disk, ffmpeg socket) stalls the entire emulation advance and any RPC contending for the locks. Contradicts BackgroundByteWriter intent.",
      "recommendation": "Snapshot active sink/writer reference under the session capture lock, release, then convert\u002BEnqueue outside all session locks. Consider TryAdd \u002B drop policy or bounded wait with fault instead of indefinite block on hot path.",
      "filePath": "src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs",
      "line": 240,
      "ruleId": "CAPTURE-TEE-BLOCKS-WORKER",
      "confidence": 0.92,
      "agent": "Grok"
    },
    {
      "severity": "medium",
      "category": "resource-lifetime",
      "title": "Stream close races writer after CompleteAndJoin timeout",
      "detail": "CompleteAndJoin(timeout) followed unconditionally by CloseQuietly on NetworkStream/FileStream and writer.Dispose. If Join times out the background thread may still be inside _write(stream.Write) or about to execute it, producing IOException/ObjectDisposed (logged as writer fault) or truncated output.",
      "recommendation": "After timeout treat as fatal for that writer (already sets faulted). Let writer own lifetime of its target stream or close only after confirmed exit. Add diagnostic when Join times out.",
      "filePath": "src/ViceSharp.Core/Media/FfmpegVideoRecorder.cs",
      "line": 484,
      "ruleId": "CAPTURE-SHUTDOWN-RACE",
      "confidence": 0.8,
      "agent": "Grok"
    },
    {
      "severity": "medium",
      "category": "resource-lifetime",
      "title": "TCP listeners left running after single-client accept",
      "detail": "videoListener/audioListener are Start()ed, one client is accepted, but listener.Stop() is deferred until full recorder Stop (or DisposeStartResources). Ports remain bound for the lifetime of the recording (and linger in error paths).",
      "recommendation": "After successful Accept for the expected single client, call listener.Stop() immediately. Only retain listener references for cleanup if the accept path itself fails.",
      "filePath": "src/ViceSharp.Core/Media/FfmpegVideoRecorder.cs",
      "line": 166,
      "ruleId": "FFMPEG-LISTENER-LIFETIME",
      "confidence": 0.75,
      "agent": "Grok"
    },
    {
      "severity": "medium",
      "category": "error-handling",
      "title": "StopCapture leaves CaptureSessions state inconsistent on finalizer exception",
      "detail": "Inside lock(SyncRoot) the code calls session.EndVideoCapture/EndAudioCapture with no try/catch. Any exception from recorder Stop (seek failure, header patch, join error) aborts before the IsActive=false update and map write, leaving a stale active entry in CaptureSessions.",
      "recommendation": "Wrap End*Capture calls in try/finally (or catch\u002Blog) so the stopped dto is always written with IsActive=false even when finalization throws.",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 278,
      "ruleId": "CAPTURE-STOP-INCONSISTENT",
      "confidence": 0.85,
      "agent": "Grok"
    },
    {
      "severity": "low",
      "category": "concurrency",
      "title": "Concurrent StartCapture(Video) for ffmpeg can start then immediately tear down the first recorder",
      "detail": "Dim snapshot and expensive Start() occur outside SyncRoot; only the final BeginVideoCapture is serialized. Two near-simultaneous muxed starts produce two ffmpeg processes and sockets; the second Begin ends the first, killing a just-launched recorder and potentially leaving a partial output file.",
      "recommendation": "Introduce a lightweight \u0027starting capture\u0027 flag or CAS under SyncRoot before launching the external process, or queue/serialize the whole start for ffmpeg kinds.",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 154,
      "ruleId": "CAPTURE-START-RACE",
      "confidence": 0.7,
      "agent": "Grok"
    }
  ],
  "runLog": {
    "path": "aiunit-results\\aiunit-review-code-20260622T135259.006Z.json",
    "startedUtc": "2026-06-22T13:52:59.0069363\u002B00:00"
  }
}
```
