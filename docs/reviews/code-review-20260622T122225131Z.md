# AI Code Review

- **Captured (UTC):** 20260622T122225131Z
- **Review kind:** code
- **Status:** fail
- **Summary:** Multiple actionable defects in resource lifetime management, concurrency around shared streams and tee points, and blocking operations under locks in the media capture implementation (FfmpegVideoRecorder, WavAudioRecorder, CaptureAudioTap, CaptureServiceHost, EmulatorRuntimeSession).

## Prompt

```text
MANDATORY FIRST STEP: read and fully follow AGENTS-README-FIRST.yaml at the repository root before reviewing - its session-start, signature/nonce verification, authentication, and MCP Server usage instructions all apply to this review. The MCP Server described in that marker is the SINGLE SOURCE OF TRUTH for requirements: pull the authoritative functional, technical, and test requirements (FR/TR/TEST) from the MCP Server requirements API (the /mcpserver/requirements/fr, /tr, and /test endpoints) and assess requirements coverage and traceability ONLY against those MCP records. FR-to-TR-to-TEST traceability is recorded in the /mcpserver/requirements/mapping endpoint (entries shaped {frId, trIds, testIds}), NOT as inline linkedFr/linkedTr fields on a TEST record; read that mapping endpoint to assess coverage, and do NOT report a TEST record as untraceable merely because it has no inline link fields - that is the expected schema, not a defect. Do NOT treat any requirements artifact outside the MCP Server as authoritative - this includes docs/Project/Functional-Requirements.md, docs/Project/Technical-Requirements.md, docs/Project/Testing-Requirements.md, docs/Project/Requirements-Matrix.md, docs/Project/TR-per-FR-Mapping.md, and any GitHub/Azure DevOps wiki export. Do NOT raise findings about coverage gaps, counts, or drift in those out-of-band documents; they are non-authoritative mirrors of the MCP Server. For Grok Build, the workflow.requirements.* names are plugin shim methods, not literal search_tool results. If dedicated requirements tools are not visible but pwsh is available, use the Grok plugin helper exactly as supported by mcpserver-grok-plugin: set GROK_PLUGIN_ROOT from the process environment or F:\GitHub\mcpserver-grok-plugin, set PLUGIN_AGENT_NAME=GrokCode, then call pwsh.exe -NoProfile -NonInteractive -File "$env:GROK_PLUGIN_ROOT\lib\repl-invoke.ps1" -Method workflow.requirements.listFr -ParamsYaml "workspacePath: F:\GitHub\vice-sharp"; repeat with workflow.requirements.listTr, workflow.requirements.listTest, and workflow.requirements.listMappings. This is the supported Grok plugin path, not a raw REST or generic helper substitute. RUNTIME ERROR CONTRACT: if authentication, subscription, quota, token/context limit, MCP access, tool permission, or any other runtime problem prevents completing the review, do not pause, wait for input, retry indefinitely, or return prose. Immediately return the same raw aiunit.review.findings.v1 JSON object with status:"error" and a single finding that explains the blocker and exact evidence. OUTPUT CONTRACT: return ONLY the raw aiunit.review.findings.v1 JSON object as your entire response - no prose preamble, no explanation, and no markdown code fences. When status is pass, findings must contain only actionable code defects in the requested review scope; do not include informational, process-only, MCP bootstrap, or trust-status notes as findings. If there are no actionable scoped code defects, return an empty findings array. Perform a code-quality, correctness, and security review of the ViceSharp media-capture surface: the capture host and recorders under src/ViceSharp.Core/Media (FfmpegVideoRecorder, FfmpegVideoEncoding, FfmpegLocator, CaptureAudioTap, WavAudioRecorder, FrameSequenceCapture, RecordingAudioBackend), src/ViceSharp.Host/Services/CaptureServiceHost.cs, and the capture wiring in src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs and src/ViceSharp.Host/Runtime/DefaultEmulatorRuntimeFactory.cs. Focus on concurrency around the emulation worker (frame/audio tee points, the TCP sockets feeding ffmpeg, lock ordering), resource lifetime (process/socket/stream disposal), and input validation. Report findings only.
```

## Response

```json
{
  "schemaVersion": "aiunit.review.findings.v1",
  "reviewType": "code",
  "status": "fail",
  "summary": "Multiple actionable defects in resource lifetime management, concurrency around shared streams and tee points, and blocking operations under locks in the media capture implementation (FfmpegVideoRecorder, WavAudioRecorder, CaptureAudioTap, CaptureServiceHost, EmulatorRuntimeSession).",
  "reviewedScope": "src/ViceSharp.Core/Media (FfmpegVideoRecorder, FfmpegVideoEncoding, FfmpegLocator, CaptureAudioTap, WavAudioRecorder, FrameSequenceCapture, RecordingAudioBackend); src/ViceSharp.Host/Services/CaptureServiceHost.cs; src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs; src/ViceSharp.Host/Runtime/DefaultEmulatorRuntimeFactory.cs. Focus: emulation worker tee paths (CommitFrame/RecordVideoFrameIfActive \u002B audio SubmitSamples via CaptureAudioTap), TCP sockets to ffmpeg, _sync/_captureSync/SyncRoot ordering, process/socket/stream disposal, input validation.",
  "agent": {
    "name": "Grok",
    "provider": "xAI",
    "model": "Grok 4.3"
  },
  "findings": [
    {
      "severity": "critical",
      "category": "resource-lifetime",
      "title": "Ffmpeg process and TCP listeners leak on Start failure",
      "detail": "CaptureServiceHost.StartVideoCapture creates FfmpegVideoRecorder and calls Start() outside any lock. Start() does Process.Start, listener.Start(), DrainAsync, then AcceptOrThrow (blocking Wait). On timeout or other InvalidOperation/IO/Arg exception from AcceptOrThrow or Process.Start wrapper, the catch returns error response without disposing the recorder. _ffmpeg, _videoListener/_audioListener, and any accepted clients remain alive (no Stop/Kill/Dispose). No try/finally in Start after acquisition.",
      "recommendation": "Ensure recorder is always disposed on any exception after construction or partial resource acquisition in StartVideoCapture (and StartAudioCapture for the FileStream path). Add try/finally inside FfmpegVideoRecorder.Start to stop listeners and kill ffmpeg on failure paths before leaving the method. Call Dispose/Stop on error before discarding the local recorder reference.",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 148,
      "ruleId": "LIFETIME-START-FAIL",
      "confidence": 0.95,
      "agent": "Grok"
    },
    {
      "severity": "high",
      "category": "concurrency",
      "title": "Data race on WavAudioRecorder output stream during Stop vs WriteSamples",
      "detail": "WavAudioRecorder.WriteSamples performs direct _output.Write/Seek-unaware writes with only a racy _stopped check and no internal lock. EndAudioCapture (RPC path) does lock(_captureSync), DetachRecorder (tap lock), then _audioRecorder.Dispose() which calls Stop() doing Seek \u002B WriteLe* \u002B Flush on the same stream. In-flight WriteSamples batch (worker thread inside tap lock at detach instant) can overlap patch writes from RPC thread. Produces corrupted RIFF sizes or file content. Contrast with FfmpegVideoRecorder which serializes all socket ops under its _sync.",
      "recommendation": "Add private locking inside WavAudioRecorder around all stream operations, _stopped, _dataBytesWritten, and the header patching in Stop. Or snapshot final size under the tap lock before detach and finalize without concurrent writers. Ensure Stop is idempotent and safe after detach.",
      "filePath": "src/ViceSharp.Core/Media/WavAudioRecorder.cs",
      "line": 75,
      "ruleId": "CONCURRENCY-WAV-STREAM",
      "confidence": 0.9,
      "agent": "Grok"
    },
    {
      "severity": "medium",
      "category": "concurrency",
      "title": "Blocking socket accept performed while holding recorder _sync lock",
      "detail": "FfmpegVideoRecorder.Start acquires lock(_sync) early, starts listeners and ffmpeg, then calls AcceptOrThrow which does task.Wait(ConnectTimeout) for the mandatory video client (up to 15s). The same lock serializes CaptureFrame, WriteSamples, Stop, and EnsureAudioStreamLocked. Audio accept task is initiated under the lock. Long blocking I/O is held under the primary synchronization object used by the emulation tee paths.",
      "recommendation": "Separate listener bind \u002B ffmpeg launch from the operational _sync lock. Perform the blocking video Accept before claiming _sync, or use a distinct initialization gate. Ensure the lock is only held for short critical sections around stream access after connections are established.",
      "filePath": "src/ViceSharp.Core/Media/FfmpegVideoRecorder.cs",
      "line": 122,
      "ruleId": "CONCURRENCY-BLOCKING-LOCK",
      "confidence": 0.85,
      "agent": "Grok"
    },
    {
      "severity": "medium",
      "category": "resource-lifetime",
      "title": "Audio FileStream and WavAudioRecorder not disposed on StartAudioCapture error after allocation",
      "detail": "In StartAudioCapture the FileStream and WavAudioRecorder are created, then lock(session.SyncRoot) \u002B BeginAudioCapture. The try covers the block but the catch for IO/Unauthorized/Argument returns error without closing the stream or disposing the recorder if an exception occurs after stream open (e.g., inside lock or tap attach). Stream handle leaks on partial failure.",
      "recommendation": "Use using or explicit dispose in finally for the FileStream when StartAudioCapture fails after allocation. Dispose the WavAudioRecorder (or ensure its Dispose is safe on incomplete state) before returning the error response.",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 185,
      "ruleId": "LIFETIME-AUDIO-START",
      "confidence": 0.8,
      "agent": "Grok"
    },
    {
      "severity": "low",
      "category": "input-validation",
      "title": "Frame length validation throws before lock in CaptureFrame hot path",
      "detail": "FfmpegVideoRecorder.CaptureFrame performs expected-length check and throws ArgumentException before acquiring _sync. This throw occurs on the emulation worker via RecordVideoFrameIfActive (which catches broadly and sets no fault flag on length mismatch). Recorder internal _faulted is only set inside the lock on IO exceptions. A mis-sized frame from video chip at start time snapshot will always throw on every frame.",
      "recommendation": "Validate size once at attach/start (already done) and/or make the length check set _faulted and return silently under the same policy as IO faults, avoiding repeated exceptions on the worker for a permanently mismatched recorder.",
      "filePath": "src/ViceSharp.Core/Media/FfmpegVideoRecorder.cs",
      "line": 231,
      "ruleId": "VALIDATION-FRAME-THROW",
      "confidence": 0.7,
      "agent": "Grok"
    }
  ],
  "runLog": {
    "path": "aiunit-results\\aiunit-review-code-20260622T121704.388Z.json",
    "startedUtc": "2026-06-22T12:17:04.3883227\u002B00:00"
  }
}
```
