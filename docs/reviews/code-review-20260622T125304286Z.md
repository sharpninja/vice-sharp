# AI Code Review

- **Captured (UTC):** 20260622T125304286Z
- **Review kind:** code
- **Status:** fail
- **Summary:** Actionable defects in concurrency (blocking I/O on worker), lock scope for audio tee, resource lifetime (no finalization on session close), allocations, and races in capture recorders and wiring.

## Prompt

```text
MANDATORY FIRST STEP: read and fully follow AGENTS-README-FIRST.yaml at the repository root before reviewing - its session-start, signature/nonce verification, authentication, and MCP Server usage instructions all apply to this review. The MCP Server described in that marker is the SINGLE SOURCE OF TRUTH for requirements: pull the authoritative functional, technical, and test requirements (FR/TR/TEST) from the MCP Server requirements API (the /mcpserver/requirements/fr, /tr, and /test endpoints) and assess requirements coverage and traceability ONLY against those MCP records. FR-to-TR-to-TEST traceability is recorded in the /mcpserver/requirements/mapping endpoint (entries shaped {frId, trIds, testIds}), NOT as inline linkedFr/linkedTr fields on a TEST record; read that mapping endpoint to assess coverage, and do NOT report a TEST record as untraceable merely because it has no inline link fields - that is the expected schema, not a defect. Do NOT treat any requirements artifact outside the MCP Server as authoritative - this includes docs/Project/Functional-Requirements.md, docs/Project/Technical-Requirements.md, docs/Project/Testing-Requirements.md, docs/Project/Requirements-Matrix.md, docs/Project/TR-per-FR-Mapping.md, and any GitHub/Azure DevOps wiki export. Do NOT raise findings about coverage gaps, counts, or drift in those out-of-band documents; they are non-authoritative mirrors of the MCP Server. For Grok Build, the workflow.requirements.* names are plugin shim methods, not literal search_tool results. If dedicated requirements tools are not visible but pwsh is available, use the Grok plugin helper exactly as supported by mcpserver-grok-plugin: set GROK_PLUGIN_ROOT from the process environment or F:\GitHub\mcpserver-grok-plugin, set PLUGIN_AGENT_NAME=GrokCode, then call pwsh.exe -NoProfile -NonInteractive -File "$env:GROK_PLUGIN_ROOT\lib\repl-invoke.ps1" -Method workflow.requirements.listFr -ParamsYaml "workspacePath: F:\GitHub\vice-sharp"; repeat with workflow.requirements.listTr, workflow.requirements.listTest, and workflow.requirements.listMappings. This is the supported Grok plugin path, not a raw REST or generic helper substitute. RUNTIME ERROR CONTRACT: if authentication, subscription, quota, token/context limit, MCP access, tool permission, or any other runtime problem prevents completing the review, do not pause, wait for input, retry indefinitely, or return prose. Immediately return the same raw aiunit.review.findings.v1 JSON object with status:"error" and a single finding that explains the blocker and exact evidence. OUTPUT CONTRACT: return ONLY the raw aiunit.review.findings.v1 JSON object as your entire response - no prose preamble, no explanation, and no markdown code fences. When status is pass, findings must contain only actionable code defects in the requested review scope; do not include informational, process-only, MCP bootstrap, or trust-status notes as findings. If there are no actionable scoped code defects, return an empty findings array. Perform a code-quality, correctness, and security review of the ViceSharp media-capture surface: the capture host and recorders under src/ViceSharp.Core/Media (FfmpegVideoRecorder, FfmpegVideoEncoding, FfmpegLocator, CaptureAudioTap, WavAudioRecorder, FrameSequenceCapture, RecordingAudioBackend), src/ViceSharp.Host/Services/CaptureServiceHost.cs, and the capture wiring in src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs and src/ViceSharp.Host/Runtime/DefaultEmulatorRuntimeFactory.cs. Focus on concurrency around the emulation worker (frame/audio tee points, the TCP sockets feeding ffmpeg, lock ordering), resource lifetime (process/socket/stream disposal), and input validation. Report findings only.
```

## Response

```json
{
  "schemaVersion": "aiunit.review.findings.v1",
  "reviewType": "code",
  "status": "fail",
  "summary": "Actionable defects in concurrency (blocking I/O on worker), lock scope for audio tee, resource lifetime (no finalization on session close), allocations, and races in capture recorders and wiring.",
  "reviewedScope": "src/ViceSharp.Core/Media (FfmpegVideoRecorder, FfmpegVideoEncoding, FfmpegLocator, CaptureAudioTap, WavAudioRecorder, FrameSequenceCapture, RecordingAudioBackend); src/ViceSharp.Host/Services/CaptureServiceHost.cs; src/ViceSharp.Host/Runtime/EmulatorRuntimeSession.cs and src/ViceSharp.Host/Runtime/DefaultEmulatorRuntimeFactory.cs. Focus: emulation worker tee paths, TCP sockets to ffmpeg, _sync/_captureSync/SyncRoot ordering, process/socket/stream disposal, input validation.",
  "agent": {
    "name": "Grok",
    "provider": "xAI",
    "model": "Grok 4.3"
  },
  "findings": [
    {
      "severity": "critical",
      "category": "concurrency",
      "title": "Blocking socket and file I/O on emulation worker thread stalls pump",
      "detail": "CaptureFrame (FfmpegVideoRecorder:316 Write to NetworkStream, FrameSequenceCapture:98 WriteBmp with per-pixel WriteByte) and audio SubmitSamples path (via CaptureAudioTap:100 or RecordingAudioBackend:58 calling Wav:102) execute under worker thread inside CommitFrame/StepInstruction (EmulationPumpService:229) and Sid6581:452. NetworkStream.Write and FileStream writes block; _captureSync or recorder _sync held during block. Stalls all emulation, audio timing, and pacing.",
      "recommendation": "Decouple capture writes via bounded queue \u002B dedicated writer thread or async IO with backpressure/drop policy. Never perform blocking I/O while holding worker or capture locks.",
      "filePath": "src/ViceSharp.Core/Media/FfmpegVideoRecorder.cs",
      "line": 316,
      "ruleId": "CAPTURE-BLOCK-WORKER",
      "confidence": 0.9
    },
    {
      "severity": "high",
      "category": "concurrency",
      "title": "CaptureAudioTap performs recorder I/O inside lock, gating downstream audio",
      "detail": "In CaptureAudioTap.SubmitSamples:87 the convert \u002B _recorder.WriteSamples occurs inside lock(_sync) before the downstream SubmitSamples at line 108. This contradicts the comment claiming live playback is never gated. When muxed ffmpeg or WAV recording active, slow capture write blocks audio output submit path (also holds tap lock during I/O). Same pattern in RecordingAudioBackend:58.",
      "recommendation": "Copy samples and perform recorder Write outside the tap lock (or use a lock-free handoff). Ensure downstream always runs with minimal latency regardless of capture state.",
      "filePath": "src/ViceSharp.Core/Media/CaptureAudioTap.cs",
      "line": 85,
      "ruleId": "TEE-LOCK-SCOPE",
      "confidence": 0.95
    },
    {
      "severity": "high",
      "category": "resource-lifetime",
      "title": "Active captures not finalized on session Close or registry removal",
      "detail": "EmulatorHostService.CloseSessionAsync:289 calls registry.Remove after only setting RunState=Stopped. No call to EndVideoCapture/EndAudioCapture, recorder.Stop/Dispose, or ffmpeg kill. CaptureServiceHost and EmulatorRuntimeSession hold _videoCapture, _audioRecorder, _audioStream, and Ffmpeg resources. On GC, TcpListener/Process/Stream lack guaranteed cleanup; ffmpeg processes and open wav files leak, producing truncated artifacts.",
      "recommendation": "In CloseSession and registry removal (or session teardown), unconditionally call End*Capture under the appropriate lock and dispose active recorders/streams/processes. Add explicit cleanup in EmulatorRuntimeSession or a wrapper.",
      "filePath": "src/ViceSharp.Host/Services/EmulatorHostService.cs",
      "line": 289,
      "ruleId": "CAPTURE-LEAK-ON-CLOSE",
      "confidence": 0.85
    },
    {
      "severity": "medium",
      "category": "resource-lifetime",
      "title": "Ffmpeg audio accept observe races with EnsureAudioStreamLocked",
      "detail": "ObserveAudioAccept:244 schedules t.Result.Close() on RanToCompletion. EnsureAudioStreamLocked:272-276 (called from WriteSamples/Stop under _sync) promotes the same client to _audioClient/_audioStream. Stop listener \u002B observe can close client after promotion or while audio stream writes are in flight (FfmpegVideoRecorder:339).",
      "recommendation": "Track ownership explicitly; only close the client from the owning path (Ensure or Stop). Use Interlocked or a disposed flag instead of fire-and-forget ContinueWith on the raw client.",
      "filePath": "src/ViceSharp.Core/Media/FfmpegVideoRecorder.cs",
      "line": 244,
      "ruleId": "AUDIO-ACCEPT-RACE",
      "confidence": 0.75
    },
    {
      "severity": "medium",
      "category": "performance",
      "title": "WavAudioRecorder allocates per WriteSamples on audio path",
      "detail": "WavAudioRecorder.WriteSamples:92 allocates \u0027byte[] chunk = new byte[ChunkBytes]\u0027 on every call while holding _sync. Invoked on every SID audio batch during recording (via tap or RecordingAudioBackend). GC pressure on capture path. Per-sample stackalloc \u002B copy also inefficient.",
      "recommendation": "Pre-allocate reusable buffer (like CaptureAudioTap._scratch). Bulk convert and write using a single span or MemoryMarshal where possible.",
      "filePath": "src/ViceSharp.Core/Media/WavAudioRecorder.cs",
      "line": 92,
      "ruleId": "ALLOC-IN-WRITE",
      "confidence": 0.8
    },
    {
      "severity": "medium",
      "category": "correctness",
      "title": "FrameSequenceCapture throws on worker path for invalid frames",
      "detail": "CaptureFrame:75-80 throws ArgumentOutOfRange/ArgumentException on negative size or length mismatch. Called from RecordVideoFrameIfActive:246 (inside pump worker under _captureSync). Ffmpeg counterpart sets _faulted silently. Although outer catch swallows, the sink does not honor the \u0027silently ignore\u0027 contract of IVideoCaptureSink.",
      "recommendation": "Silently ignore (or fault internally like Ffmpeg) instead of throwing from CaptureFrame. Validate once at BeginVideoCapture.",
      "filePath": "src/ViceSharp.Core/Media/FrameSequenceCapture.cs",
      "line": 79,
      "ruleId": "SINK-THROW-WORKER",
      "confidence": 0.8
    },
    {
      "severity": "low",
      "category": "correctness",
      "title": "Missing validation and hard-coded audio params for recorders",
      "detail": "CaptureServiceHost.StartAudioCapture:203 and FfmpegVideoRecorder ctor defaults hard-code 44100/1. No cross-check against actual SID SamplingRate or configured backend. TargetPath used for CreateDirectory/FileStream/ffmpeg argv with only whitespace check (lines 88, 199). FfmpegLocator.IsAvailable rescans PATH on every call.",
      "recommendation": "Snapshot actual sample rate/channels at capture start from the tap or architecture. Add basic path canonicalization or at least document trust boundary. Cache ffmpeg locate result.",
      "filePath": "src/ViceSharp.Host/Services/CaptureServiceHost.cs",
      "line": 203,
      "ruleId": "HARDCODED-AUDIO-PARAMS",
      "confidence": 0.7
    }
  ],
  "runLog": {
    "path": "aiunit-results\\aiunit-review-code-20260622T124522.466Z.json",
    "startedUtc": "2026-06-22T12:45:22.4665638\u002B00:00"
  }
}
```
